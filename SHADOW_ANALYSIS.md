# SHADOW_ANALYSIS.md — почасовой разбор теневого контура (ночная сессия #2)

Источник данных: `journal/shadow_signals.json` (GitHub Contents API, тот же приватный
репо, путь отдельный от `journal/signals.json`) — пишется `shadow_engine.log_shadow_async()`
из `signal_loop._send_alert()` и `pump_detector._confirm_pump_reversal()` при каждом
живом сигнале/pump-реверсале, см. `SHADOW_MODE.md`. Формат: список записей с полями
`patches_affected`/`discrepancy` (Блок 1) либо `type: "pump_reversal_shadow"` (Блок 4).

Протокол правды: запись за час пишется ТОЛЬКО если есть реальные новые данные за этот
час. Если новых записей нет — честная строка "нет новых shadow-записей", без выдуманной
статистики на пустых данных.

---

## 2026-07-11 00:30 (первая проверка)

`git show origin/main:journal/shadow_signals.json` → `fatal: path does not exist in
'origin/main'`. Файл ещё не создан на GitHub — контур запущен этой ночью (коммит
`f678297`), живого сигнального алерта или pump-реверсала с момента деплоя ещё не было
(либо Railway ещё не подхватил новый код — не проверено, доступа к Railway API/CLI в
этой сессии нет, см. `AUDIT.md`/более ранние записи PROGRESS.md о неавторизованном
`railway status`). Нет новых shadow-записей за этот час — не выдумываю статистику.

## 2026-07-11 01:23 (почасовой цикл)

Тот же результат: `git show origin/main:journal/shadow_signals.json` →
`fatal: path does not exist in 'origin/main'`. По-прежнему ноль живых записей —
честно, нет новых shadow-записей за этот час. Параллельно этой ночью (сессия #3,
Блок A/B) получен ИСТОРИЧЕСКИЙ (не live-shadow) аналог этого сравнения —
`backtest/engine_patched.py` реплеит боевую и патченую (01+02 как гейт, 03/04/05 как
теги) логику на реальных прошлых данных, результат — `PATCH_IMPACT.md` (когда прогон
завершится). Это НЕ заменяет живой shadow-контур (разные источники данных, разная
цель — исторический бэктест против накопления реальных будущих сигналов), но даёт
первый содержательный сигнал по патчам, пока живых shadow-записей ещё нет.

## 2026-07-11 08:00 — ВЕРДИКТ: почему `shadow_signals.json` пуст за ~10 часов

По прямому запросу владельца. **Только диагностика — код не менялся, ничего не
задеплоено.** Раньше (00:30) не было доступа к Railway CLI — сейчас `railway` был
доступен и авторизован (`railway whoami` → `tdgv2n7ch9@privaterelay.appleid.com`),
привязал репозиторий к проекту `resilient-respect` / сервису `crypto-bot`
(`railway link -p resilient-respect`, окружение `production`) и прочитал живые логи
(`railway logs --since Nh`, read-only, ничего не задеплоено и не перезапущено).

**Вердикт: shadow-контур ЗАДЕПЛОЕН и технически исправен, работает не только
локально.** Пуст журнал не из-за деплоя — из-за архитектурной несвязанности с
единственным реально работающим источником сигналов. Доказательства:

1. **Процесс жив на Railway**: `railway status` → сервис `crypto-bot`, статус
   `● Online`. Живые логи показывают точную строку `[AUTO] итог: {N} лонг, {M} шорт
   (из {A}/{B} кандидатов прескрина)` — это буквально `bot.py:4672-4673` в текущем
   репозитории, значит деплой не устарел относительно кода, который мы читаем.
2. **APScheduler реально тикает**: в логах видны регулярные `Running job
   "_heartbeat_wrapper..."` на интервалах 2/5/15/30 минут и `run_watchdog` каждые
   10 минут — фоновые задачи не висят и не падают в краш-луп.
3. **`shadow_engine.log_shadow_async()` подключён РОВНО в двух местах** (проверено
   `grep` по репозиторию): `signal_loop.py:350` (внутри `_send_alert()`, вызывается
   только когда Ступень 1 + Ступень 2 `signal_loop`-контура нашли реальный сетап) и
   `pump_detector.py:1256` (внутри `_confirm_pump_reversal()`, вызывается только на
   стадии "REVERSAL CONFIRMED", уже ПОСЛЕ первичного детекта пампа/дампа).
4. **Ни один из этих двух путей не сработал ни разу за ночь**:
   - `railway logs --since 10h --filter "signal_loop"` → **0 строк**. `_log()` в
     `signal_loop.py` — обычный `print(f"[signal_loop] {msg}")`, то есть попадает в
     stdout/Railway-логи гарантированно, если вызывается. Ноль строк означает: за
     40 тиков Ступени 1 (15-мин интервал × 10ч) ни разу не было ни кандидата
     (`_log` при неудаче Ступени 2 тоже сработал бы), ни исключения (`_log(f"stage1
     exception..."`) тоже молчит) — Ступень 1 (funding-экстремум/OI-сёрдж/sweep)
     стабильно не находит НИ ОДНОГО кандидата, каждый раз. Не крэш — тихий ноль по
     дизайну (`if not candidates: return`, без лога).
   - `railway logs --since 10h --filter "REVERSAL"` и `--filter "Pump Radar"` →
     **0 строк**. В логах видны только ранние стадии детекта (`DUMP detected
     (silent): CASHCAT -5.94%` и т.п.) — это ДО стадии reversal-confirmation, где
     подключён shadow. Стадия подтверждения разворота не достигалась ни разу.
5. **Живой журнал (`journal/signals.json`, синхронизируется каждый час с этого же
   Railway-процесса) это подтверждает независимо**: 90 записей на 07:41, источники —
   `TOP_SHORT_AUTO` (45), `TOP_LONG_AUTO` (39), `TOP_SPOT_AUTO` (6). **Источника
   `"signal_loop"` — НОЛЬ записей.** Это тот самый доминирующий источник
   (`send_scheduled()`, `bot.py:4527`, интервал 30 мин) — и в его коде НЕТ ни одного
   упоминания `shadow_engine` (`grep -n "shadow_engine" bot.py` → 0 совпадений в
   теле `send_scheduled`). Он просто не вызывает контур вообще, независимо от того,
   сработала бы Ступень 1/2 `signal_loop` или нет.

**Итог одной фразой**: контур не мёртв и не "только локальный" — он живёт на
Railway ровно там, где его подключили (`signal_loop`/`pump_detector`), но эти два
пути за всю ночь не произвели ни одного события, а путь, который реально
производит 100% сигналов (`send_scheduled`/`TOP_*_AUTO`), к контуру физически не
подключён. Это закрывает вопрос `ROADMAP_2W_DRAFT.md` п.1.3 ("проверить, почему
signal_loop/pump_radar пока не оставили записей"), открытый с 2026-07-10 —
"тихо не работает" (Ступень 1 слишком строгая/не находит кандидатов) плюс
архитектурный разрыв (AUTO не зовёт shadow), не деплой-баг.

**Что НЕ проверено (честно, для полноты)**: сам раунд-трип записи в GitHub
(`journal/shadow_signals.json` через Contents API) при имплементации (Блок 1,
2026-07-10) был явно помечен как "не проверен живым прогоном" — этот риск остаётся
теоретически открытым, но сейчас непроверяем, так как код до него ни разу не
дошёл (0 срабатываний). Не проверялась глубина логов дальше `--since 10h` (Railway
CLI отдаёт снапшот, не полную историю по времени старта контура в 2026-07-10) —
возможен (не подтверждён) редкий случай срабатывания раньше этого окна, но живой
журнал показывает 0 записей `signal_loop` за все известные 90 записей вообще, не
только за 10 часов, что делает эту вероятность низкой. **Ничего не исправлено** —
варианты (расширить критерии Ступени 1, подключить `shadow_engine` к
`send_scheduled`, или оставить как есть) — решение владельца.

## 2026-07-11 ~19:50 UTC — Пакет 3 М4: INSIGHTS по накопленной shadow-статистике

По заданию владельца (НОЧЬ#2 Блок 12): "сколько shadow-сигналов есть, расхождения
с боевыми, паттерны" — теперь, когда прошло больше времени и патч 01 в бою
(с 2026-07-11 11:38:34, см. `backtest/patch01_live_vs_shadow.py`). Данные получены
ЖИВЬЁМ через GitHub Contents API (`railway run` для реального `GITHUB_TOKEN`, тот
же паттерн, что и весь Пакет 2/3) непосредственно перед записью этого раздела —
не из локальной копии (локальная копия `journal/shadow_signals.json` в этом
чекауте не обновляется git'ом, см. ниже).

**Сколько shadow-сигналов есть**: 4 записи всего, ВСЕ типа `pump_reversal_shadow`
(Блок 4, `pump_detector.py:_confirm_pump_reversal()`). Записей формата "5-патчей"
(Блок 1, поля `patches_affected`/`discrepancy`/`rr_tp1_live` из `SHADOW_MODE.md`)
— **ноль, по-прежнему**. Это НЕ новый баг и не регресс — это то же самое
архитектурное расхождение, что уже доказано в разделе 08:00 выше: 100% реальных
сигналов идут через `send_scheduled()`/`TOP_*_AUTO`, который физически не вызывает
`shadow_engine`; `signal_loop.py` (единственный код, который зовёт 5-патчевый
`log_shadow_async()`) как не находил ни одного кандидата на Ступени 1, так и не
находит. **Перепроверено свежо**: живой журнал сейчас 94 записи (было 90 на
07:41) — прирост 4 сигнала, все снова `TOP_SHORT_AUTO`/`TOP_LONG_AUTO`/
`TOP_SPOT_AUTO`(+3 новых `X100`, ранее не встречался как source — не расследовал,
не в скоупе этого пункта), `railway logs -n 5000 | grep -c signal_loop` → **0** —
идентично 08:00-вердикту, ничего не изменилось за прошедшие ~12 часов.

**Расхождения live vs shadow**: сравнивать НЕЧЕГО для 5-патчевого контура — нет ни
одной пары "боевой сигнал + shadow-запись к нему" (`live_journal_id` ни разу не
заполнен ни в одной из 4 записей — все они pump-reversal-формата, где этого поля
вообще нет). Отдельно, патч 01 (killzone-часы) в бою: с момента промоушена
(11:38:34) до сейчас (~8.2ч) в живом журнале **0 новых сигналов вообще** (см.
Пакет 2 М3, `PATCH_IMPACT.md`) — значит и сравнивать live-исход с shadow-часами
пока не на чем, окно всё ещё не набрало данных, это не сбой.

**Паттерны в 4 pump-reversal записях (честно: n=4, статистически незначимо,
это качественное наблюдение, не вывод)**:
- 3 из 4 — символ `busdt` (все SHORT, retrace 6.7-14.5%, volume_mult 16-44x) — либо
  повторный сигнал по одной и той же монете в течение ~3.5 часов (ts разброс
  1783772288→1783784470), либо совпадение на маленькой выборке — не различить
  на 4 записях, не додумываю причину.
- Штраф "Dead Zone" (`DEAD_ZONE_SHADOW_SCORE_PENALTY=10`, находка владельца по
  карточке EVAA, см. `shadow_engine.py:47`) активен в 2 из 4 записей
  (`kz_quality="D"`). В ОБЕИХ штраф не изменил бы боевое решение: запись #2
  (`pro_score_live=40`) и так ниже `PROMOTE_SCORE_THRESHOLD=60`
  (`pump_detector.py:63`) без штрафа; запись #3 (`pro_score_live=85`,
  `promoted_live=true`) остаётся выше порога даже С штрафом (85-10=75≥60). На
  этой выборке (n=4) штраф Dead Zone ни разу не изменил бы исход промоушена —
  честно: это НЕ довод ни за, ни против штрафа, выборка слишком мала, чтобы
  судить о его пользе.

**Вывод**: свежих данных недостаточно для содержательной shadow vs live
статистики патчей 01-05 через живой контур — единственный работающий шадоу-канал
(pump-reversal) дал 4 качественных наблюдения, ни одно не критично. Архитектурный
разрыв `send_scheduled`↔`shadow_engine` (описан в 08:00-вердикте) остаётся
единственной причиной пустоты 5-патчевого канала — решение (чинить или оставить)
по-прежнему за владельцем, этот пакет его не принимает.

## 2026-07-11 23:31 (Стамбул) — почасовой цикл, проверка (б)

`git show origin/main:journal/shadow_signals.json` → те же **4 записи**, что и в
проверке Пакета 3 М4 (последняя — `busdt`, ts `1783784470.05` = 2026-07-11
15:41:10 UTC). **Нет новых shadow-записей за этот час** — честно, не выдумываю
статистику на пустых данных. Расхождений боевой vs патченой логики разбирать
не на чем (0 новых событий).

## 2026-07-12 03:23 (Стамбул) — почасовой цикл, проверка (б) — КОНТУР ОЖИЛ

**Архитектурный разрыв, описанный в разделе "08:00 — ВЕРДИКТ" выше, закрыт.**
Пакет 4 М2 (подключение `shadow_engine` к `send_scheduled()`, деплой
подтверждён живьём ~23:50 вчера) реально работает: `journal/shadow_signals.json`
теперь **192 записи** (было 4) — прирост **+188 за ночь**, все с
`"source": "send_scheduled"`. Новых записей за последний час (окно
00:23-01:23 UTC) — **46**.

**Разбор 188 send_scheduled-записей**:
- **`promoted_live`: 0 из 188 (0%)** — честно, ни один кандидат в этом окне не
  прошёл ВСЕ боевые гейты. Не интерпретирую это как проблему гейтов без
  дополнительных данных — окно короткое (~6ч с деплоя), рынок мог просто не
  давать качественных сетапов; нужно больше времени для содержательного
  вывода.
- **Причины отказа (`gate_reasons`, сумма >188 -- у кандидата может быть
  несколько)**: `rocket_or_grade` — 184/188 (98%, доминирующая причина),
  `rr_gate` — 154/188 (82%), `rsi_extreme_long` — 20, `rsi_extreme_short` — 6,
  `suspicious_volume` — 4.
- **Патчи 03/04/05 на этой же выборке (post-factum тег, не гейт)**:
  02-rr-gate(shadow, порог 2.0) задел бы 162/188 (86%) — ожидаемо больше, чем
  live-гейт (1.5), раз почти все и так уже отсеяны live-гейтом 1.5. 03-breaker-
  mitigation — 108/188 (57%). 05-bpr confluence — 35/188 (19%). 04-rsi-
  дивергенция против направления — 13/188 (7%). Патч 01 (killzone) не даёт ни
  одного расхождения — ожидаемо, live-часы и shadow-часы сейчас совпадают
  (патч 01 уже в бою с Пакета 4 М3).

**Конкретный пример** (THETA, long, ts 1783802743): `rr_tp1_live=0.75` —
отсеян live-гейтом (`rr_gate` в `gate_reasons`) и shadow-гейтом тоже
(`shadow_rr_gate_pass: false`, порог 2.0). Одновременно `bpr_confluence: true`
(5 BPR-зон, зона входа пересекается со свежей) и `breaker_mitigation:
"mitigation"` — оба патча пометили бы эту сделку, если бы R:R прошёл, но он
не прошёл ни в одном из двух гейтов, так что расхождение чисто теоретическое
для этой конкретной сделки.

**Итог**: контур теперь реально накапливает данные каждые 30 минут (интервал
`send_scheduled`) — впервые появилась содержательная выборка для будущей
статистики по патчам 02-05 в реальном потоке кандидатов (не только по
прошедшим все гейты, как раньше). 0% promoted в первом окне — честно
зафиксировано, не интерпретирую как вывод без большего окна.

## 2026-07-12 04:23 (Стамбул) — почасовой цикл, проверка (б)

Всего записей: **265** (было 192, +73 за этот час, окно 00:23-01:23 UTC), все
`source: send_scheduled`.

- **`promoted_live`: 1 из 73 (первый живой промоушен с момента подключения
  контура в Пакете 4 М2)** — конкретный пример: `BEAT` short, `rr_tp1_live:
  1.53` — прошёл live-гейт (порог 1.5) НАРОДУ, но НЕ прошёл бы shadow-гейт
  02 (порог 2.0) — `discrepancy: "R:R 1.53 прошёл live-гейт (1.5), но НЕ
  прошёл бы shadow-гейт (2.0)"`. Также помечен патчем 03 (`breaker_mitigation:
  "breaker"`). Живой пример расхождения live vs shadow на РЕАЛЬНО
  промоутнутой сделке, не только на отброшенных.
- **Причины отказа остальных 72**: `rocket_or_grade` — 67/73 (92%), `rr_gate`
  — 61/73 (84%), `suspicious_volume` — 4, `rsi_extreme_long/short` — по 2.
  Картина та же, что в прошлом часу (доминирует rocket/grade-гейт).
- **Патчи на этой выборке**: 02-rr-gate(shadow) задел бы 63/73 (86%),
  03-breaker-mitigation — 46/73 (63%), 05-bpr — 13/73 (18%), 04-rsi-
  дивергенция — 8/73 (11%) — пропорции стабильны относительно прошлого часа.

**Накопительно за 2 часа работы контура**: 265 записей, 1 promoted (0.4%) —
выборка всё ещё маленькая для статистических выводов по эффекту патчей на
win-rate, но сам факт накопления работает как задумано.

## 2026-07-12 05:23 (Стамбул) — почасовой цикл, проверка (б)

Всего записей: **316** (было 265, +51 за час, окно 01:23-02:23 UTC), все
`source: send_scheduled`. **0 promoted в этом часу** — картина ближе к
первому часу (0/188), чем ко второму (1/188), пока недостаточно данных
судить о типичной частоте промоушена (нужно больше окон для стабильной
оценки).

Патчи/гейты — пропорции стабильны относительно предыдущих часов:
`rocket_or_grade` 49/51 (96%), `rr_gate` 47/51 (92%), `02-rr-gate`(shadow)
48/51 (94%), `03-breaker-mitigation` 28/51 (55%), `05-bpr` 13/51 (25%),
`04-rsi-дивергенция` 8/51 (16%). Ничего качественно нового относительно уже
описанной картины — не повторяю конкретные примеры, накопление идёт как
задумано.

**Накопительно за 3 часа**: 316 записей, 1 promoted (0.3%).

## 2026-07-12 06:23 (Стамбул) — почасовой цикл, проверка (б)

**316 → 367 (+51 за час, окно 02:23-03:23 UTC)**, 0 promoted. Пропорции
патчей/гейтов те же: `rocket_or_grade`/`rr_gate` доминируют (46/51 и 44/51),
`02-rr-gate`(shadow) 46/51, `03-breaker-mitigation` 37/51, `05-bpr` 16/51,
`04-rsi-дивергенция` 5/51. Без нового качественного паттерна — накопление
идёт стабильно тем же темпом (~50-73 записей/час).

**Накопительно за 4 часа**: 367 записей, 1 promoted (0.27%).

## 2026-07-12 -- INSIGHTS v2 (Пакет 5 М5) -- первый содержательный разбор по 417 записям send_scheduled

По заданию владельца: "то, что Блоку 12 не хватало" — распределение по
гейтам, сколько кандидатов режет каждый патч 02-05, топ расхождений
live/shadow с примерами. Данные — живой `git show origin/main:journal/
shadow_signals.json`, 421 запись всего (4 `pump_reversal_shadow` + **417
`send_scheduled`**), проверено непосредственно перед записью.

### Распределение по боевым гейтам (из 417 кандидатов)

| Гейт | Отсеял | % |
|---|---|---|
| `rocket_or_grade` (rocket>=60 + грейд A+/A/B) | 396 | 95.0% |
| `rr_gate` (R:R>=1.5) | 351 | 84.2% |
| `rsi_extreme_long` | 26 | 6.2% |
| `suspicious_volume` | 14 | 3.4% |
| `rsi_extreme_short` | 13 | 3.1% |

**Главный вывод**: `rocket_or_grade` — безоговорочно доминирующий фильтр
(95% кандидатов), R:R-гейт на втором месте (84%). Вместе они объясняют
303/417 (73%) всех отказов (комбинация `rocket_or_grade`+`rr_gate` —
самая частая пара причин). RSI-экстремум и подозрительный объём — на
порядок реже, скорее точечные случаи, чем системный фильтр.

### Сколько кандидатов из 417 задел бы каждый патч 03-05 (post-factum тег, не гейт)

| Патч | Задел бы | % |
|---|---|---|
| 02-rr-gate (shadow, порог 2.0) | 364 | 87.3% |
| 03-breaker-mitigation | 252 | 60.4% |
| 05-bpr confluence | 93 | 22.3% |
| 04-rsi-дивергенция против | 39 | 9.4% |

Пропорции устойчивы во всех почасовых чекпоинтах этой ночи (23:31-06:23) —
не артефакт одного часа, накопительная картина за ~8ч подтверждает те же
соотношения.

### Топ расхождений live vs shadow — конкретные примеры (не повтор уже описанного)

1. **BEAT short** (уже в чекпоинте 04:23) — единственный **promoted_live=true**
   кандидат: R:R 1.53 прошёл live-гейт (1.5), НЕ прошёл бы shadow-гейт (2.0)
   — прямая иллюстрация цены разницы порогов 1.5 vs 2.0 на реально
   промоутнутой сделке.
2. **DEXE long** — R:R live 2.0, что автоматически ПРОХОДИТ и shadow-гейт
   тоже (2.0 >= 2.0) — этот кандидат отсеялся ТОЛЬКО по `rocket_or_grade`,
   при этом помечен патчем 03 (`breaker_mitigation: "breaker"`). Показывает:
   даже кандидат, который прошёл бы ОБА R:R-гейта, всё равно не пройдёт без
   достаточного rocket/грейда — гейт `rocket_or_grade` доминирует независимо
   от R:R.
3. **ESP short** — самый насыщенный BPR-пример за ночь: `bpr_zone_count=16`
   (пересечений зоны входа с BPR-зонами), но R:R live всего 0.71 — отсеян и
   `rocket_or_grade`, и `rr_gate` с большим запасом, несмотря на сильный
   BPR-confluence сигнал патча 05.
4. **LAB short** — пример патча 04: `bullish_classical: true` дивергенция
   ПРОТИВ направления шорта — отсеян только `rocket_or_grade` (R:R не был
   проблемой), дивергенция добавляет предостережение поверх уже отклонённого
   гейтом кандидата.

### Честно, что НЕ видно на этой выборке

0 promoted-кандидатов не позволяют содержательно сравнить win-rate live vs
shadow на РЕАЛЬНЫХ исходах сделок (только 1 promoted за всю ночь, исход
сделки ещё не известен — `live_journal_id` в записи `null`, `signal_journal`
использует другой источник для `send_scheduled`-сигналов, сопоставление
исхода отдельно не реализовано в этом пакете). Статистика по патчам выше —
про то, СКОЛЬКО кандидатов патч бы задел, не про то, был бы он прав.
Понадобится больше суток и связка с фактическими исходами сделок (TP/SL) для
содержательного вывода о полезности патчей 02-05, не только о частоте
срабатывания.

## 2026-07-12 -- Пакет 7 М2: связка с фактическими исходами -- инфраструктура готова, первое сравнение честно 0/20

Владелец решение "ДА": построена связка `journal/shadow_signals.json` <->
`journal/signals.json` (`shadow_outcome_analysis.py`, 14 тестов,
`PROGRESS.md` Пакет 7 М2). Для НОВЫХ promoted-кандидатов `send_scheduled()`
теперь пишет реальный `live_journal_id` напрямую при создании записи (было:
всегда `null`); для старых записей -- ретроактивный матчинг по
`(symbol, direction, окно 10 мин)`.

**Первый прогон на живых данных (607 shadow-записей, 96 journal-записей)**:

Недостаточно данных: **0/20** исходов среди promoted-live сигналов с
сопоставленной shadow-записью. Причина -- честно диагностирована, не просто
"мало времени прошло":

- Всего **5** shadow-записей с `promoted_live=true` за всё время (контур
  подключён к `send_scheduled` недавно, промоушенов пока мало).
- 1 (`busdt`, старый тип `pump_reversal_shadow` до Пакета 4) -- отдельная
  схема без `live_journal_id`, не участвует в этой связке по дизайну.
- 1 (`BEAT` short) -- совпадение НАЙДЕНО по времени (разница ~1.9с), но
  исход сделки ещё `PENDING` (`outcome: null`) -- корректно не считается,
  не выдумываю исход.
- 2 (`TRUMP` short, `2Z` long) -- совпадения НЕ найдено вообще: в
  `journal/signals.json` нет записи с этим symbol/direction ни в окне
  времени, ни где-либо ещё. Похоже на молчаливый сбой `signal_journal.log_
  signal()` в `send_scheduled()` на момент промоушена (защищено try/except,
  ошибка ушла только в Railway-логи, retroактивно не восстановить) -- ИМЕННО
  этот класс проблемы Пакет 7 М2 устраняет для будущих записей (прямой id
  вместо восстановления по времени).

**Вывод**: инфраструктура готова и протестирована, работает корректно (не
показывает выдуманных чисел на недостаточной выборке). Реального сравнения
пока нет -- честно ждём накопления promoted-сигналов с известным исходом до
20. Отчёт будет пересчитан и дополнен сюда, когда порог будет достигнут.

## 2026-07-12 -- Пакет 8 М4: повторная проверка -- всё ещё 0/20, честно

Владелец допускал, что порог 20 может быть достигнут "в этом пакете" --
перепроверено на живых данных (631 shadow-запись, 96 journal-записей, было
607/96 на момент Пакета 7 М2): **promoted_live всё ещё 5** (тот же набор --
`busdt`, `BEAT`, два `2Z`, `TRUMP`), новых promoted-кандидатов с момента
деплоя фикса М2 не появилось. Причина понятна и не тревожна: `send_scheduled`
крутится раз в 30 минут, а промоушенов накопилось всего 5 за ВСЁ время работы
этой связки (несколько дней) -- за час-два между Пакетами 7 и 8 новых
promoted-сигналов физически неоткуда взяться. **Честно: 20 исходов не
наберётся в рамках одной рабочей сессии** -- это вопрос дней, не часов,
инфраструктура (Пакет 7 М2) к этому готова и сработает автоматически, как
только данные накопятся.

## 2026-07-12 23:42 -- Ночной конвейер, почасовой цикл (б): аномалия найдена и диагностирована живьём

Задача цикла: проверить новые shadow-записи с прошлого часа. Формальный ответ
("новых записей нет") был бы НЕЧЕСТЕН здесь -- разрыв слишком большой и слишком
объяснимый, чтобы промолчать о причине.

**Наблюдение**: GitHub-копия `journal/shadow_signals.json`
(`git show origin/main:...`) стоит на **997 записях**, последняя запись
`ts=1783864775.145138` = **2026-07-12 16:59:35 EEST** -- на момент проверки
(23:23-23:42 EEST) это **~6.5 часа** без единой новой записи, хотя за это время
должно было пройти несколько 30-минутных циклов `send_scheduled()`.

**Диагностика (не догадка -- живые логи + живой повторный запрос)**:

1. `railway logs --json` подтвердил: `[AUTO]`-цикл ЖИВ и крутится по расписанию
   -- `19:51:50` и `20:21:50` UTC (`22:51`/`23:21` по Стамбулу), оба раза
   `итог: 0 лонг, 0 шорт` (из 5/17 и 2/12 кандидатов прескрина
   соответственно). Это НЕ падение процесса и не завис scheduler.
2. В логах цикла `19:51` (тот, что вёл 5 кандидатов через
   `real_full_analysis()`) нашёлся РЕАЛЬНЫЙ сбой: в 19:52:55 UTC --
   burst из ~18 пар `shadow_engine: GitHub GET failed (Expecting value: line 1
   column 1 (char 0))` + `shadow_engine: GitHub PUT failed (422 ... "sha"
   wasn't supplied)`, все в окне **~3 миллисекунд**. GET на GitHub Contents
   API вернул пустое/непарсящееся тело -> `sha` не извлёкся -> PUT ушёл без
   `sha` -> GitHub его отклонил как невалидный запрос (422, а не 409-конфликт,
   который штатно ловится повтором в коде).
3. **Важно**: `_write_local()` (локальная запись) вызывается и проверяется
   ДО попытки GitHub-синка (`shadow_engine.py:513-515` -- `if not ok_local:
   return False`), и раз GET/PUT-сообщения вообще напечатались, значит
   локальная запись для этих кандидатов УЖЕ прошла успешно -- само по себе
   падение GitHub-синка не роняет и не откатывает локальную копию, только
   мешает ей попасть в GitHub-зеркало в этот момент. Это ожидаемо по дизайну
   модуля (см. докстринг `shadow_engine.py:11-13` -- "Railway ephemeral,
   GitHub best-effort").
4. Живой повторный тест ПРЯМО СЕЙЧАС (`railway run`, тот же GET-запрос к
   `contents/journal/shadow_signals.json`): статус 200, валидный JSON,
   `X-RateLimit-Remaining: 4975/5000` -- лимит не при чём, запрос отработал
   штатно. Второй `[AUTO]`-цикл (20:21-20:22 UTC) НЕ породил ни одной
   GitHub-ошибки в логах -- значит инцидент был единичным, не хронической
   поломкой кода.
5. Честно НЕ удалось проверить: содержит ли ЛОКАЛЬНАЯ копия на живом
   контейнере записи новее 997 -- `railway run` поднимает СВЕЖИЙ отдельный
   контейнер из того же образа (без персистентного volume), а не
   подключается к уже работающему процессу, поэтому его локальный
   `journal/shadow_signals.json` -- это копия из последнего деплоя, а не
   живое файловое состояние работающего бота (та же ловушка, что и с
   `bot._last_auto_scan` в предыдущих циклах -- см. `PROGRESS.md`).

**Вывод (честно, без додумывания)**: разрыв в GitHub-копии -- НЕ признак
падения AUTO-цикла (он жив и работает по расписанию) и, судя по всему, НЕ
хронический баг в коде синхронизации (единичный транзиентный сбой сети/GitHub
API, самоустранившийся к следующей попытке). Но это ЗНАЧИТ, что до
следующего успешного `_sync_to_github_sync()` любые локально накопленные с
19:52 записи существуют ТОЛЬКО на живом контейнере и будут потеряны при
редеплое (Railway ephemeral, без volume) -- ни подтвердить, ни исключить
их наличие оттуда я сейчас не могу. Рекомендация (не внедрено, только
предложение): следующий пакет мог бы добавить принудительный ретрай-синк
(например, в начале следующего `send_scheduled()`-цикла -- "досинхронизировать
всё, что накопилось локально, но не ушло в GitHub") или алерт владельцу при
подряд идущих сбоях PUT, чтобы такие временные окна не оставались незамеченными.
Код синхронизации НЕ менялся в рамках этого цикла (только диагностика,
согласно правилам ночи -- код меняется отдельным шагом с py_compile/pytest,
не на лету посреди диагностики).

## 2026-07-13 -- Пакет 14: методология сравнения real_full_analysis() vs 13-блочный tz13_shadow-вердикт

Владелец, п.6: как будем сравнивать `bot.real_full_analysis()` (боевой AUTO-путь)
с новым независимым `ta_extra.build_13block_verdict()` (`tz13_shadow` в записях
`journal/shadow_signals.json`, поле `tz13_*` на верхнем уровне записи -- `score`/
`direction`/`setup_type`/`entry_zone`/`sl`/`tp1-3`, см. `shadow_engine.compute_shadow()`).
Ниже -- ПРЕДЛОЖЕНИЕ (пороги/метрики), решение о переводе чего-либо в бой -- за
владельцем, здесь ничего не внедряется в живой путь.

**Что уже даёт честное сравнение "из коробки"** (без дополнительного кода --
оба направления уже пишутся в одну запись):
- `direction` (боевой `is_long`) vs `tz13_direction` -- согласны ли движки по
  стороне сделки на одном и том же тике.
- `rr_tp1_live` (боевой) vs `tz13_shadow.block11_tp_rr.rr_tp1` -- разница в
  оценке R:R одной и той же ситуации разными методами построения зон
  (боевой `real_full_analysis()` строит SL/TP по `swing`/`support`/`resistance`/
  ATR, tz13 -- по `ta_extra.find_sr_zones()`/`build_trade_from_structure()`,
  та же методология, что уже использует `fa_engine.py`).
- `promoted_live`/`gate_reasons` (уже пишутся `send_scheduled()`, Пакет 4 М2)
  vs `tz13_shadow.block13_verdict.has_setup` -- случаи, где боевой путь
  ОТКЛОНИЛ кандидата, а tz13-чеклист (6/6 методики авторского курса) считает сетап
  готовым, и наоборот -- это самое интересное расхождение для разбора.

**Метрики для отчёта** (после накопления, см. порог ниже):
1. **Directional agreement rate** -- % записей, где `direction == tz13_direction`
   (среди записей, где оба определены, т.е. НЕ NEUTRAL/None ни у одного).
2. **Setup-type distribution** -- частота AMD/SH-BOS-RTO/Sweep/Cypher/None среди
   `tz13_setup_type` -- честная проверка, реально ли различает классификатор
   разные ситуации, или скатывается в один доминирующий тип (если, скажем,
   90%+ записей -- Sweep, это сигнал, что пороги SH-BOS-RTO/AMD слишком узкие
   и классификатор не то чтобы "реально независимый вердикт").
3. **R:R divergence** -- `abs(rr_tp1_live - tz13_rr_tp1)` распределение
   (медиана/p90) -- насколько по-разному два метода видят одну и ту же зону.
4. **has_setup rate у tz13 vs promoted_live rate** -- какой путь строже/мягче
   (если tz13 пропускает 80% кандидатов, а боевой -- 20%, это сразу видно без
   привязки к фактическому исходу).
5. **Win-rate (WR) по закрытым сделкам** -- для каждого движка ОТДЕЛЬНО: доля
   случаев, когда TP1 был достигнут раньше SL. Считается симуляцией
   форвардного прохода цены по свечам ПОСЛЕ `ts` записи -- тот же принцип,
   что уже даёт `shadow_outcome_analysis.py` для других shadow-полей (Пакет 7
   М2, связка через `live_journal_id`), применяется к tz13 через
   `tz13_entry_zone`/`tz13_sl`/`tz13_tp1`. Считается только там, где у
   движка есть направление И entry/sl/tp1 все не `None` (у tz13 это
   подмножество записей с `block13_verdict.has_setup == true` -- по первым
   28 живым записям такие есть, см. живая верификация ниже).
6. **Profit Factor (PF)** -- по тем же закрытым сделкам: сумма R выигрышных
   сделок / |сумма R проигрышных| (R = `(exit - entry) / (entry - sl)` в
   направлении сделки). Считается отдельно для боевого пути и для tz13 --
   единственная метрика здесь, которая учитывает НЕ ТОЛЬКО частоту побед, но
   и их размер (WR 40% с PF>1.5 может быть лучше WR 55% с PF<1).

**Порог до отчёта/решения** (не внедрено -- ориентир, как у `oi_funding_ls_shadow`/
Патча 09, владелец: "копится минимум 3 суток ИЛИ 100 сигналов, что раньше"):
**минимум 100 записей с непустым `tz13_shadow` (`tz13_score is not None`) ИЛИ
5 суток с момента деплоя, что раньше**. 5 суток (не 3, как у Патча 09)
предложено потому, что здесь сравниваются ДВА направления + тип сетапа + R:R
одновременно (более многомерная картина, единичные дни могут случайно
совпасть по направлению на трендовом рынке и скрыть реальные расхождения по
типу сетапа/R:R) -- но якорное число записей то же самое, 100, по прямому
ориентиру владельца.

**Пороги вердикта** (предложение, ПОСЛЕ накопления n -- финальное решение о
переводе чего-либо в бой не мной):
- **tz13 «лучше»**: WR(tz13) − WR(боевой) ≥ +5 п.п. **И** PF(tz13) ≥
  PF(боевой) × 1.1 -- оба условия вместе, не одно (иначе шум от малого n
  легко даёт ложный "лучше" по одной метрике). Тогда есть смысл рассмотреть
  частичный перевод в бой (например, alert владельцу при сильном
  расхождении `direction`, или tz13 `setup_type` как доп. фильтр к боевому
  чеклисту) -- решение всё равно за владельцем, это только основание для
  предложения.
- **tz13 «хуже»**: WR(tz13) − WR(боевой) ≤ −5 п.п. **ИЛИ** PF(tz13) <
  PF(боевой) × 0.9 -- tz13 остаётся чисто исследовательским/shadow, сначала
  дорабатывается упрощённая методология (см. честный список упрощений
  блоков 2/3/9 в отчёте по DoD пакета), потом переоценка на новом n.
- **«Неотличимо»**: разница WR в пределах ±5 п.п. **и** PF в пределах ±10%
  -- оба движка статистически неразличимы на этом n, нужен больший n
  (следующий порог -- 300 записей) или более узкий срез (например, только
  записи, где `tz13_setup_type` конкретный -- проверить, не работает ли tz13
  лучше НА ОДНОМ типе сетапа, даже если в среднем неотличим).
- **Честная оговорка**: ±5 п.п. на n=100 -- практический, не строгий
  статистический порог (доверительный интервал WR на 100 наблюдениях шире
  5 п.п. при типичной дисперсии) -- при пограничном результате порог сам по
  себе не решение, а сигнал "смотреть отчёт внимательнее", не автоматический
  вердикт.

**Что дальше** -- по достижении порога: отдельный отчёт (не в этом файле,
отдельный `.md`, по аналогии с `PATCH_IMPACT.md`) с посчитанными метриками
1-6 выше и вердиктом по порогам, `INSIGHTS.md`-запись, и явный вопрос
владельцу: "перевести ли что-то из tz13 в боевой путь (или хотя бы alert при
сильном расхождении direction)". **Решение о live -- исключительно за
владельцем**, эта сессия только собирает данные и предлагает методологию
измерения.

---

## НОЧЬ#3, Н1 -- срез shadow-статистики, 2026-07-13T23:18:41+03:00

Владелец, открытие НОЧЬ#3: "Shadow-статистика к утру: свежие срезы по всем
контурам -- tz13 (n после деплоя Пакета 14, совпадение направления с AUTO,
дельта score), Патч 05 BPR (n/200), Патч 09 (n/100), EMA-стек (окно 3 суток
с починки потока). Только цифры, решения мои." Скрипт --
`tools/night3_shadow_stats.py` (11 тестов, `tests/test_night3_shadow_stats.py`),
читает `journal/shadow_signals.json` (1890 записей на момент прогона), пороги
и метрики -- та же методология, что уже зафиксирована выше в этом файле
(Пакет 14 DoD), не изобретена заново.

### tz13 (Пакет 14, 13-блочный shadow-движок)

- Окно: с деплоя Пакета 14 (`815c3e8`, 2026-07-13T15:40:54+03:00) по текущий момент.
- Всего shadow-записей send_scheduled в этом окне: 535.
- Записей с непустым `tz13_score` (tz13 реально досчитал): **n=394**.
- Совпадение направления (live `direction` vs `tz13_direction`, только где оба определены): 233/296 = 78.7%.
- Распределение `tz13_score` (чеклист 0-6): среднее 2.53, медиана 2.0, мин 1, макс 5.
  (честная оговорка: у live-пути `real_full_analysis()` нет прямого числового аналога tz13-чеклиста для дельты "score - score" -- сравнивается распределение самого tz13_score, не разница с чем-то.)
- `has_setup=true` у tz13: 33/394 = 8.4%.
- `promoted_live=true` (боевой путь реально отправил сигнал) в том же окне: 0/394 = 0.0%.
- Распределение `tz13_setup_type`: AMD: 257 (65.2%), SH-BOS-RTO: 73 (18.5%), None: 53 (13.5%), Sweep: 8 (2.0%), Cypher: 3 (0.8%).
- |R:R live − R:R tz13| (n=296): медиана 0.00, p90 0.20.
- Статус порога отчёта: **порог 100 записей ИЛИ 5 суток с деплоя ДОСТИГНУТ** (n=394 ≥ 100).
- WR/PF по закрытым сделкам -- НЕ считаются в этом срезе (нужна отдельная форвардная симуляция по свечам после `ts`, как в `shadow_outcome_analysis.py` -- не входит в объём этого ночного блока, честно не выдумываю числа).

### Патч 05 -- BPR (Balanced Price Range) confluence

- Записей с посчитанным `bpr_zone_count`: **n=1881** (порог владельца: 200).
- `bpr_zone_count`: среднее 4.9, медиана 5.0, макс 19.
- `bpr_confluence=true` (свежий BPR пересекается с зоной входа): 423/1881 = 22.5%.
- Записей, где BPR попал в `discrepancy`: 423/1881 = 22.5%.
- Статус порога: **порог 200 ДОСТИГНУТ**.

### Патч 09 -- OI/Funding/L-S shadow

- Записей с посчитанным `oi_funding_ls_shadow` (без ошибки): **n=840** (порог владельца: 100).
- `total_delta` (насколько изменился бы Rocket Score, если бы OI/funding/L-S подключили к боевому скорингу): среднее +1.32, медиана +0.0, диапазон [-4, +10].
- Знак дельты: 378 положительных (45.0%), 95 отрицательных (11.3%), 367 нулевых (43.7%).
- Статус порога: **порог 100 ДОСТИГНУТ**.

### EMA-стек shadow (окно 3 суток с починки потока)

- Починка потока (print->log.error + health-счётчик): коммит `347c0a7`, 2026-07-13T10:34:19+03:00.
- Окно 3 суток от починки: с этого момента по 2026-07-16T10:34:19+03:00.
- **Честно: с момента починки прошло ~12.7 часов из 72 -- окно ещё НЕ закрыто**, срез выше -- на текущий момент, не финальный отчёт.
- Записей `type=ema_stack_shadow` в прошедшей части окна: **n=1**.
  - DEXE: score 84->84, направление long->long, diverges=False
- Структурная причина низкого n (не обязательно поломка): `log_ema_stack_shadow_async()`
  вызывается только внутри `_confirm_pump_reversal()` -- редкое событие
  (подтверждённый разворот памп/дамп), не на каждый AUTO-сигнал. За всю
  историю файла (1890 записей) до починки накопилась всего 1 такая запись --
  сравнение объёма "было/стало" будет иметь смысл только после накопления
  заметно большего числа pump/dump-реверсалов.

**Итог Н1**: tz13/Патч 05/Патч 09 -- пороги отчёта достигнуты, цифры выше.
EMA-стек -- окно ещё открыто (12.7 из 72 часов), n=1, следующий осмысленный
срез -- не раньше 2026-07-16. Решения по переводу чего-либо в бой -- за
владельцем, эта запись только цифры.

## 2026-07-19 15:47 -- срез shadow-статистики + дельта к 2026-07-13 (после #285-b фикса)

Владелец, поручение после live-verify #285-b: "следующий приоритет --
shadow-аналитика (пересчёт исходов по контурам)". Тот же скрипт --
`tools/night3_shadow_stats.py`, тот же источник (`journal/shadow_signals.json`,
локальная копия из git, коммит `f367fc2d`, синхронно с origin/main на момент
прогона). Дельты ниже -- к предыдущему замеру 2026-07-13T23:18:41+03:00 (см.
выше), это первый пересчёт с тех пор -- 6 суток, объём вырос ~5.6x (1890 ->
10644 записей).

### tz13 (Пакет 14)

- n (send_scheduled в окне с деплоя Пакета 14): 535 -> 10644 (полный файл,
  все записи после деплоя, скрипт считает весь файл как "окно").
- Записей с `tz13_score`: 394 -> **3310** (+2916).
- Совпадение направления live/tz13: 78.7% -> **79.3%** (+0.6пп, стабильно).
- `tz13_score` среднее: 2.53 -> 2.72, медиана 2.0 -> 3.0.
- `has_setup=true`: 8.4% -> **11.1%** (+2.7пп).
- `promoted_live=true`: 0.0% (0/394) -> **0.7% (23/3310)** -- ЗАМЕТНОЕ
  ИЗМЕНЕНИЕ: на прошлом срезе не было ни одного случая, где боевой путь
  реально прошёл гейты в том же окне, что и tz13-кандидат; теперь 23 таких
  случая накопилось. Только факт, не рекомендация к промоушену -- это
  решение владельца (Класс C).
- Распределение `tz13_setup_type` заметно сдвинулось: AMD 65.2% -> 45.3%
  (-19.9пп), None (нет паттерна) 13.5% -> 26.6% (+13.1пп), Sweep 2.0% ->
  14.0% (+12.0пп), SH-BOS-RTO 18.5% -> 12.9% (-5.6пп), Cypher 0.8% -> 1.1%.
  Честно: не интерпретирую причину сдвига (рыночный режим сменился за 6
  суток или сам микс монет в автосканах другой) -- нужен отдельный разбор,
  если это важно владельцу.
- |R:R live - R:R tz13| медиана стабильно 0.00, p90 0.20 -> 0.17.

### Патч 05 -- BPR confluence

- n: 1881 -> **4569** (+2688).
- `bpr_confluence=true`: 22.5% -> **22.6%** (+0.1пп, стабильно -- доля
  BPR-подтверждений не изменилась при росте выборки в 2.4x, хороший знак
  устойчивости метрики).

### Патч 09 -- OI/Funding/L-S shadow

- n: 840 -> **4387** (+3547).
- `total_delta` среднее: +1.32 -> **+1.65** (небольшой рост -- OI/funding/L-S
  сигнал в среднем чуть сильнее повышал бы Rocket Score, чем на прошлом срезе).
- Знак дельты: положительных 45.0% -> 49.1% (+4.1пп), отрицательных 11.3% ->
  15.2% (+3.9пп), нулевых 43.7% -> 35.7% (-8.0пп) -- сигнал реже даёт "0",
  чаще склоняет счёт в ту или иную сторону, чем раньше.

### EMA-стек shadow

- 3-суточное окно (с 2026-07-13T10:34:19+03:00) теперь **ЗАКРЫТО**
  (72.0/72 часов на момент прогона `night3_shadow_stats.py`, финальный
  прогон скрипта -- не интерим, окно физически истекло 2026-07-16).
- Итог окна: **n=8** записей `type=ema_stack_shadow` (было n=1 на момент
  предыдущего среза). Все 8: `diverges=False` -- ни разу EMA-стек НЕ разошёлся
  с "старым" направлением/счётом в сторону, которая изменила бы вывод (AKE
  x4, BANK x4, LAB x1, B x1 -- итого 10 строк, часть символов повторяется).
  Честно: n=8 за 3 суток подтверждает структурную причину низкого объёма
  из прошлой записи (событие редкое, привязано к подтверждённым pump/dump-
  реверсалам) -- статистической значимости для решения по EMA-стеку это
  количество не даёт, дальнейшее накопление продолжается без гейта на само
  наблюдение.

**Итог среза**: все 4 контура растут ожидаемо, ни одной структурной
поломки не найдено (BPR confluence % и |R:R| дельта стабильны при кратном
росте выборки -- признак того, что shadow-движок считает consistently, не
шумит). Единственная содержательная перемена -- `promoted_live` tz13
перешёл 0 -> 23 случаев и сдвиг `tz13_setup_type` в сторону Sweep/None --
обе находки только зафиксированы, решения не мои. EMA-стек 3-суточное окно
закрыто честно (n=8, малая выборка, `diverges=False` везде).

### Закрытые исходы по контурам (`shadow_outcome_analysis.closed_outcomes_report()`)

Первый прогон этой конкретной функции, зафиксированный в этом файле --
прежних чисел для дельты нет. **Честная оговорка по методологии** (из
докстринга `closed_outcomes_by_contour()`, дословно): контурные строки
(tz13/П05/П09) -- это НЕ "как отработал бы контур, если бы решал сам"
(отдельной форвардной симуляции по собственным entry/SL/TP контура сейчас
нет, вопреки более ранней методологической заметке выше в этом файле про
"форвардный проход по свечам через tz13_entry_zone/tz13_sl/tz13_tp1" --
это было предложение методологии, не то, что реализовано в коде сейчас) --
а "как отработали РЕАЛЬНО ОТПРАВЛЕННЫЕ (promoted_live) сделки, для которых
этот контур ТОЖЕ был посчитан" -- то есть подмножество live-исходов,
отфильтрованное по наличию поля, не независимый счёт.

Таблица ниже -- **ПОСТ-ФИКС** состояние (после дедупа по `journal_id`, см.
ниже); до фикса те же числа читались по строкам (20/20/14/14 вместо
15/15/12/12) -- сырые "по строкам" числа сохранены в тексте ниже для
истории находки.

| контур | закрыто (сделок) | WR | PF | порог (20) |
|---|---|---|---|---|
| live (все сделки) | 15 | 20.0% | 0.58 | не достигнут (-5) |
| tz13 | 12 | 16.7% | 0.38 | не достигнут (-8) |
| patch05_bpr | 15 | 20.0% | 0.58 | не достигнут (-5, =live -- BPR-поле посчитано у 100% закрытых live-сделок) |
| patch09_oi | 12 | 16.7% | 0.38 | не достигнут (-8) |

**Значимая находка (честно, без прикрас)**: у "live" (реально отправленные
сигналы с известным терминальным исходом) на n=20 -- WR 20.0%, PF 0.63 (PF
< 1 = в сумме R проигрышных сделок больше, чем R выигрышных, за этот
конкретный срез). n=20 -- МАЛАЯ выборка (доверительный интервал WR на 20
наблюдениях очень широкий, по методологии выше даже на n=100 порог ±5пп
"практический, не строгий статистический"), не делаю вывод "боевой путь
не работает" из 20 сделок -- честно фиксирую цифру и малость выборки.
tz13/patch09_oi показывают тот же (даже чуть хуже) результат, но это то же
самое подмножество тех же 20 сделок (14 из них), не независимая проверка
контура -- ожидаемо похожи на "live", не самостоятельный сигнал "tz13
хуже". patch05_bpr тождественен "live" ровно потому, что все 20 закрытых
сделок имеют посчитанный `bpr_zone_count` -- не отдельный результат,
дублирование по конструкции функции.

Решения (что-либо переводить в бой, менять гейты) -- не мои, только цифры
и честная методологическая оговорка, что именно они измеряют.

**Дополнительная методологическая находка (ретро-проверка "n=20")**: 20
"закрытых" строк в таблице выше -- это НЕ 20 разных сделок. Одна и та же
живая сделка (`journal_id`) может встретиться НЕСКОЛЬКО раз, если
`send_scheduled`/`signal_loop` пере-логировали shadow-снимок того же ещё
не закрытого сигнала на нескольких циклах ДО его закрытия -- в текущей
выборке 20 строк = **15 РАЗНЫХ сделок** (5 "лишних" строк: `journal_id`
100 -- 2 строки, 101 -- 3, 156 -- 3). `closed_outcomes_by_contour()`
считает WR/PF по СТРОКАМ, не по уникальным `journal_id` -- сделки с
несколькими shadow-снимками взвешиваются сильнее.

Пересчёт по уникальным сделкам (n=15, дедуп по `journal_id`, первое
вхождение): **WR 20.0% (3/15)** -- случайно СОВПАДАЕТ с числом по строкам
(4/20=20.0%). **PF 0.58** -- уже НЕ совпадает с отчётным 0.63 (хуже, не
лучше -- в этой конкретной выборке многократно залогированные сделки не
искажали WR в лучшую сторону, но PF сдвинули).

**ФИКС ПРИМЕНЁН (владелец, "ДА", 2026-07-19 16:xx)**: `_dedup_by_trade()`
добавлена в `shadow_outcome_analysis.py`, вызывается в
`closed_outcomes_by_contour()` сразу после сборки `matched` -- один
shadow-снимок на `journal_id` (последний по `ts`), гейт `min_outcomes`
теперь считается по уникальным сделкам. `format_closed_outcomes_lines()`
своей логики подсчёта не имеет (только форматирует уже готовый `report`
dict) -- фикс распространяется на 08:30-сводку автоматически, отдельного
изменения там не потребовалось. Тесты: `test_dedup_by_trade_keeps_latest_snapshot_per_trade`,
`test_closed_outcomes_by_contour_dedups_repeated_trade_snapshots`,
`test_closed_outcomes_by_contour_dedup_gate_counts_unique_trades_not_rows`
(фикстура повторяет форму реальных данных: 20 строк/15 сделок/3 win).
Живой пересчёт после фикса: `live` closed=15, WR 20.0%, PF **0.58**,
`ready=false, remaining=5` (было бы ошибочно `ready=true` до фикса на
20 строках) -- см. таблицу выше, уже обновлена итоговым (пост-фикс)
состоянием.

**Setup-type по уникальным сделкам** (n=15, честно -- n на группу слишком
мал для вывода, только сырые цифры): `None/не классифицирован`: 2/8 win
(25%), `Sweep`: 0/4 win (0%), `AMD`: 1/3 win (33%). Не делаю вывод "Sweep
хуже" -- на n=3-8 на группу доверительный интервал шире самой разницы.

**Что НЕ сделано сегодня (честно, не в объёме этого среза)**: полный
ретро-разбор каждой из 15 закрытых сделок через `METHODOLOGY_CORE.md`
(какие фильтры чеклиста/SMC сработали бы, что улучшило бы вход/SL/TP)
требует точечной докачки свечей по каждому символу на момент сигнала --
отдельная по объёму задача, следующий пункт вечного бэклога (см. CLAUDE.md
"Режим непрерывной очереди", пункт 3), не сделан в рамках этого среза.

### Обновление 2026-07-21 -- ещё 2 сделки закрылись, критерий промоушена зафиксирован

Владелец (ответ на 08:30-бриф): контуры остаются в тени, **критерий
промоушена окончательно** -- 20 закрытых УНИКАЛЬНЫХ исходов с PF>1 (не
объём shadow-записей). Свежий пересчёт `closed_outcomes_report()`
(данные подросли с позавчерашних 15 до **17** уникальных сделок -- NIGHT
TP1_HIT и ZEC SL_HIT закрылись за прошедшие ~1.5 суток):

| контур | закрыто | WR | PF | до гейта (20) |
|---|---|---|---|---|
| live | 17 (было 15) | 23.5% (было 20.0%) | **0.78** (было 0.58) | осталось 3 (было 5) |
| tz13 | 13 (было 12) | 23.1% (было 16.7%) | **0.62** (было 0.38) | осталось 7 (было 8) |
| patch05_bpr | 16 (было 15) | 25.0% (было 20.0%) | **0.78** (было 0.58) | осталось 4 (было 5) |
| patch09_oi | 13 (было 12) | 23.1% (было 16.7%) | **0.62** (было 0.38) | осталось 7 (было 8) |

Тренд позитивный (WR и PF выросли по всем контурам), но **PF всё ещё <1
везде** -- честно, не round up до "почти готово". `live` ближе всего к
гейту (осталось 3 закрытия). Следующий пункт очереди -- ретро-разбор
этих 17 сделок через `METHODOLOGY_CORE.md` (владелец подтвердил приоритет
над любым новым пакетом, см. `NEXT_PACKAGE.md`).
