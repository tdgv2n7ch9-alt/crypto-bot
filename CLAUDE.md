# CLAUDE.md — crypto-bot

Проект BEST TRADE crypto-bot. Текущее состояние, архитектура и известные проблемы — см.
`AUDIT.md`. План улучшений по приоритетам — `ROADMAP.md`. Журнал сессий и рабочие конвенции
(git, бэкапы БД, аддитивные миграции, постоянные границы) — `PROGRESS.md`. Карта модулей и
файлов (что есть и как устроено, сверено построчно с кодом) — `DOCUMENTATION.md`.

## Правила владельца — трёхуровневая система решений

Владелец выстроил систему принятия решений в три уровня. Это верхний, действующий всегда контекст
для всей остальной работы над проектом (ритуалов, протокола правды, границ ниже).

- **Уровень 1 — Claude Code (эта роль).** Добывает решения и обязан ДОКАЗЫВАТЬ каждое. Любое
  предложение должно выдерживать вопрос «покажи источник» — прямая ссылка на первоисточник + что
  именно там сказано. Не выдержало — снимается без обсуждений.
- **Уровень 2 — внешняя проверка.** Значимые предложения (новые механики, цены, платежи, всё
  связанное с деньгами и сигналами) владелец перепроверяет через независимый анализ. Предложения
  готовятся так, чтобы выдержать проверку вторым экспертом: первоисточники, а не пересказы; свежие
  данные с датами; работающие внедрения, а не анонсы.
- **Уровень 3 — владелец.** Финальное решение всегда за ним. Claude Code предлагает и доказывает —
  владелец решает.

Что это значит для ежедневной работы:

1. **Протокол правды — не формальность, а основа доверия ко всей системе.** Одна выдуманная ссылка
   или приукрашенная сводка обесценивает недели честной работы.
2. **«Выполнено» = протестировано.** Слово «готово» без прогона — нарушение.
3. **Честное «не знаю» / «не проверено» / «сломалось» — профессионализм, а не слабость.** За
   правду о проблеме претензий не будет никогда; за скрытую проблему — будут.
4. **Серьёзность задач максимальная — живые продукты с реальными людьми и реальными деньгами.**
   (У владельца два таких продукта: медицинская Академия — репутация врача и обучение
   специалистов; этот крипто-бот — финансовые решения подписчиков.) Цена небрежности здесь
   измеряется не багами, а доверием людей.
5. **Планка — мировой уровень, каждый день.** Разведка, `INSIGHTS.md`, «Предложение дня» —
   обязательный ритуал, не опция.

## Пакетный ритм

Работа идёт ПАКЕТАМИ, не сменами. Пакет = набор модулей, каждый доводится до
определения готовности (DoD): код + тесты + `py_compile` отдельным шагом + полный
`pytest` без регрессий + коммит/пуш + подтверждённый деплой Railway (новый
deployment ID, не SKIPPED) + живая проверка через логи контейнера или ответ
бота (см. «Верификация деплоя» ниже) с фиксацией в `PROGRESS.md`.

Внутри пакета — полная автономия в границах: боевая логика/токены/рассылка
подписчикам — только с явного «да» владельца; аддитивность; два падения → SKIPPED;
чекпоинт в `PROGRESS.md` после каждого модуля.

Модуль, не доведённый до DoD, в отчёт идёт как **НЕ ГОТОВ с причиной** — не как
«почти готово».

Конец пакета: отчёт (готово/SKIPPED/находки/вопросы) + проект следующего пакета в
`NEXT_PACKAGE.md` с оценкой цены/ценности каждого модуля. Стоп до приёмки
владельцем.

### Верификация деплоя

**Все будущие пуши — только через `tools/deploy.sh`** (владелец, ПАКЕТ
deploy-resilience, 2026-07-14): rebase-retry push → определение, затронуты ли
Watch Paths (`tools/deploy_watch_check.py`, живое чтение `railway.json`) →
пауза 3 мин → сверка коммита контейнера → авто-триггер при неожиданном
SKIPPED → результат в `logs/deploy.log` и Telegram владельцу. Ручной
`git push` для боевого кода — только если `tools/deploy.sh` сам недоступен
(например его исполняемость сломана) и это зафиксировано в PROGRESS.md как
исключение.

Найдено живьём (Пакет SECURITY-HARDENING, SEC М4, 2026-07-13): `railway run`
выполняет команду ЛОКАЛЬНО на машине разработчика (с подставленными
Railway-переменными окружения), а НЕ в свежем удалённом контейнере. Прошлые
«живые проверки» этим методом валидны как проверка кода+секретов, но НЕ как
проверка задеплоенного контейнера — ретро-переоценку не делаем (код
идентичен задеплоенному), но метод меняем на будущее.

Стандарт живой верификации деплоя (часть DoD):

1. **Статус деплоя** — `railway deployment list` показывает SUCCESS + новый
   deployment ID (не SKIPPED) для актуального коммита.
2. **Доказательство из САМОГО контейнера** — `railway logs` с ожидаемой
   строкой (стартовая диагностика/лог-маркер в коде при необходимости), ИЛИ
   проверка через самого бота в Telegram (команда владельца → ответ бота =
   рантайм подтверждён). Одного статуса деплоя недостаточно.
3. **`railway run` допустим ТОЛЬКО для проверок, которые ничего не пишут во
   внешние хранилища** (GitHub Contents API, журналы и т.п.). Запись из
   локального запуска запрещена — именно так тестовый мусор из локального
   `pytest`-прогона утёк в боевой `journal/security_log.json` на GitHub в
   SEC М4 (найдено и исправлено в том же чекпоинте, см. `PROGRESS.md`).
4. **Фильтровать секреты из вывода HTTP-логгеров** (владелец, найдено живьём
   2026-07-15, П-Каналы): `python-telegram-bot`/`httpx` логируют ПОЛНЫЙ URL
   каждого запроса на уровне INFO, а Telegram Bot API кладёт токен прямо в
   URL (`https://api.telegram.org/bot<TOKEN>/...`) — прямой вызов через
   `railway run`/`railway ssh`, использующий `Bot`/`Application` из
   `python-telegram-bot`, печатает токен в вывод команды, даже если сам код
   его нигде не логирует явно. Перед любым таким вызовом —
   `logging.getLogger("httpx").setLevel(logging.WARNING)` (или эквивалент
   для используемого HTTP-клиента) ДО первого запроса. `requests`-based
   вызовы (как во всех остальных живых проверках в этом файле) эту проблему
   не имеют — логгер `httpx` актуален только когда в скрипте явно
   импортируется `telegram.Bot`/`Application`.
5. **Для чтения `journal/*.json` (или любого другого файла контейнера) —
   ТОЛЬКО `railway ssh`, не `railway run`** (владелец, найдено живьём
   2026-07-15, ежедневная гигиена): `railway run` выполняется ЛОКАЛЬНО (см.
   п.3 выше) — если в рабочей директории разработчика существует локальная
   копия того же пути (например `journal/shadow_signals.json` от прошлой
   ручной синхронизации/pull), команда read-only тихо прочитает ЕЁ, а не
   живой файл контейнера, и вернёт правдоподобные, но неверные числа без
   единой ошибки — обнаружено на числах размера/счётчика shadow-записей,
   разошедшихся между `railway run` (локальная копия) и `railway ssh`
   (реальный контейнер). Правило п.3 ("`railway run` допустим для read-only
   проверок") остаётся в силе для случаев, где по построению нет риска
   одноимённого локального файла (например `git`-операции на самом
   репозитории) — но для journal-файлов и любых артефактов, которые могут
   существовать локально под тем же путём, только `railway ssh`.

Утренняя сводка 08:30 и «Метрика дня» 21:00 в боте остаются — они от бота, не от
сессии, к пакетам не привязаны.

## Постоянный контур 24/7

Правило владельца (2026-07-14): очередь работы не пустеет сама по себе — сессия
всегда знает, что делать дальше, без ожидания нового поручения.

**1. Приоритет очереди** (всегда в этом порядке):
   а) регрессы/баги, найденные владельцем в живой приёмке;
   б) пакеты дня, поставленные владельцем/планировщиком;
   в) shadow-контуры и их срезы (накопление статистики, не переключение live);
   г) фоновые задачи (библиотека, транскрибация, on-chain и т.п.);
   д) если всё выше пусто — следующий модуль `NEXT_PACKAGE.md` / роадмап Фаз B–L
      по порядку, shadow-first.

**2. Точки владельца** — решения, которые сессия никогда не принимает сама,
   только копит факты в `PROGRESS.md` и утренний бриф: промоушен shadow→live,
   изменения боевых гейтов/скоринга, эталоны текстов/UX, состав эталона
   разметки (например v1.1), любые вопросы денег/API-подписок.

**3. Ритм суток**:
   - 08:30 — утренняя сводка в Telegram + `output/MORNING_BRIEF_<дата>.md`
     (`tools/morning_brief.py`): ночные итоги, статусы зон, созревшие решения,
     топ-3 вопроса владельцу.
   - День — пакеты по приёмке владельца и разметкам (приоритет «а»/«б» выше).
   - Вечер — планировщик заряжает ночную очередь.
   - Ночь — очередь + фоновые задачи под `night_run.sh`.

**4. DoD каждого блока** (см. «Пакетный ритм» выше) — без исключений даже в
   фоновом режиме: `py_compile` отдельным шагом → `pytest` → деплой при
   необходимости → живая верификация → чекпоинт в `PROGRESS.md`. Ошибки — только
   через `log.error`. Деплой падает дважды подряд — откат последнего коммита,
   фиксация причины в `PROGRESS.md`, продолжение со следующего блока очереди.

**5. Ежедневная гигиена** (дешёвые проверки в хвост каждой ночи, до утренней
   сводки): (а) хвост ошибок контейнера за сутки — новые ТИПЫ ошибок (не
   повторы известных) в бриф; (б) размер `journal/*.json` — аномальный рост в
   бриф; (в) все запланированные job'ы отработали за сутки по логам — пропуски
   в бриф; (г) счётчик shadow-n по всем контурам в бриф.

**6. Ночной режим — упор в gate не останавливает сессию** (владелец,
   2026-07-16, действует с этой ночи): gate-задачи (деплой в боевой код,
   изменение боевых данных, всё, что помечено «стоп до приёмки») ночью
   по-прежнему НЕ исполняются. Но упор в такой gate — это переключение на
   **ночную фоновую очередь** (read-only анализ, docs, тесты, планы/оценки —
   без деплоя и без изменения боевых данных), а не остановка сессии. Утром —
   сводка «сделано ночью» ОДНИМ блоком в бриф 08:30, затем возврат к
   gate-задачам на приёмку владельца. Каждый пункт ночной очереди всё равно
   получает свой DoD (там, где применимо: py_compile/pytest/чекпоинт в
   `PROGRESS.md`) — «ночной» не значит «без проверки», значит «без деплоя».

## Автономия v2 — классификация задач A/B/C (владелец, 2026-07-16)

Каждая задача в очереди относится к одному из трёх классов. Классификация
определяет, что сессия делает сама, а что ждёт владельца — независимо от
времени суток (день/ночь), но действует ПОВЕРХ, не ВМЕСТО правила «Ночной
режим» выше: если конкретная ночь получила отдельное явное указание
«деплои запрещены» — это указание для той ночи главнее общей автономии
класса B.

- **Класс A (авто, без приёмки)**: read-only, docs, тесты, DRY-RUN,
  конспекты — ничего боевого не меняет и не деплоит. Исполняется сразу,
  отчёт идёт в очередной бриф (не требует отдельного «стоп»).
- **Класс B (авто-DoD)**: код-фиксы ИЗВЕСТНОГО паттерна (тот же класс
  бага/структуры, что уже был решён и проверен раньше — не новая
  архитектура, не новый механизм) с machine-checkable DoD. Чек-лист,
  все 5 пунктов обязаны быть зелёными С ДОКАЗАТЕЛЬСТВОМ (не «сделал»,
  а прикреплённый вывод команды):
  1. `py_compile` OK
  2. полный `pytest` без регрессий
  3. `tools/deploy.sh` -> SUCCESS (не SKIPPED, если ожидался боевой деплой)
  4. чистый рестарт по `railway logs` (Application started, ни одного
     traceback)
  5. 60 минут без watchdog-алертов/новых traceback после деплоя
  Все 5 зелёных = задача закрыта БЕЗ владельца, сводка (тег
  `[AUTO-DoD]` + полный чек-лист с цифрами) — в бриф. Любой пункт
  красный ИЛИ сомнение в том, зелёный ли он = стоп, класс C (gate).
- **Класс C (только владелец)**: деньги, сделки, промоушены shadow→live,
  изменение/удаление боевых данных, новая архитектура, новые внешние
  сервисы/подписки, любое необратимое действие. Стоп до приёмки, как и
  раньше.

**Правило эскалации**: сомнение в классификации задачи = класс C.
Занижать риск нельзя, завышать (лишний раз спросить владельца) можно.

**Калибровочный период**: первую неделю действия правила каждая
авто-закрытая задача класса B в брифе помечается тегом `[AUTO-DoD]` с
полным чек-листом (все 5 пунктов, с цифрами/выводом команд) — владелец
калибрует доверие к автономии на фактах, не на обещаниях.

## Ритуал «Планка дня» — ежедневная конкурентная разведка

При старте каждой рабочей сессии, ДО перехода к задачам:

**1) РАЗВЕДКА (web search, 10-15 мин)** — что нового у лидеров по трём направлениям:
- **Сигнальные и аналитические сервисы**: ведущие crypto-signal платформы и аналитические
  терминалы (Glassnode, Santiment, CryptoQuant, TradingView alerts, топовые Telegram-сигнальные
  боты) — какие метрики дают, как подают сигналы, как показывают track record.
- **Данные и инфраструктура**: обновления API бирж (Binance, Bybit), новые источники рыночных
  данных, изменения rate-limit и гео-политик, надёжные зеркала.
- **Методология**: проверенные практики оценки качества сигналов (win-rate по режимам,
  R-мультипли, drawdown-метрики, честный track record) от признанных квант-источников.

**2) ФИЛЬТР** — только рабочее и проверенное (не хайп, не «100x-стратегии»). У каждой находки:
кто применяет, что даёт, применимо ли к нашему боту.

**3) ЖУРНАЛ `INSIGHTS.md`** — запись за день: дата, 2-4 находки, источники, вердикт:
«внедряем / адаптируем / не наше и почему».

**4) ПРЕДЛОЖЕНИЕ ДНЯ** — 1-2 лучшие находки в конце сводки, с оценкой трудозатрат и эффекта.

## Протокол правды

Обязателен для разведки («Планка дня»), `INSIGHTS.md` и любых утверждений о внешнем мире
(конкуренты, API, цены, метрики) — а также, в части пп. 4-6, для любых сводок о состоянии этого
проекта. Нарушение протокола — ошибка более высокого приоритета, чем любая недоделанная фича.

1. **ФАКТ = ИСТОЧНИК.** Каждое утверждение о конкурентах, фичах, API, ценах, метриках — только с
   прямой ссылкой на первоисточник (официальный сайт, документация, блог компании, релизноты).
   Нет ссылки — факт не записывается вообще.
2. **ПРОВЕРКА ПЕРЕД ЗАПИСЬЮ.** Прежде чем внести находку в `INSIGHTS.md` — открыть источник через
   web search/fetch и убедиться, что там написано именно это. Пересказ по памяти запрещён: память
   может устареть или ошибаться.
3. **МАРКИРОВКА УВЕРЕННОСТИ.** Каждая запись помечается: `[проверено — ссылка]` /
   `[не удалось проверить]` / `[устаревшее, дата]`. Непроверенное не попадает в «Предложение дня»
   никогда.
4. **ЗАПРЕТ УГОДЛИВОСТИ.** Не подгонять выводы под ожидания владельца. Если находка противоречит
   нашим планам — писать как есть. Если данных недостаточно — писать «данных недостаточно», а не
   додумывать. Честное «не знаю» всегда лучше красивой догадки.
5. **ЧЕСТНОСТЬ О СВОЁМ.** В сводках и `PROGRESS.md`: что работает — подтверждено тестом (каким);
   что не проверено — так и помечено «не проверено»; что сломано — сказано прямо. Запрещено
   называть задачу выполненной без прогона.
6. **ЦИФРЫ И ОБЕЩАНИЯ.** Никаких выдуманных процентов эффекта («это даст +40% конверсии»). Если
   эффект неизвестен — «эффект не измерен, узнаем после внедрения по метрике X».

## Железные границы (постоянные, применяются всегда, включая разведку)

- Торговую/сигнальную логику, пороги и формулы (чек-лист, R:R-гейт, TP/SL/entry, критерии
  stage1/stage2) **не менять без явного одобрения владельца**. Разведка по методологии и новым
  источникам только **предлагает** — никогда не внедряет сама.
- Не трогать ключи API бирж.
- Ничего не рассылать реальным подписчикам.
- `dr_linessa_bot` — отдельный проект, не трогать.
- **Разметки владельца ("галочка автора") — лучший сигнал в системе.** Живут в
  `journal/watch_zones.json` с `tier: "author"`, отдельный раздел меню «⭐ ЛИМИТКИ» (Пакет 18,
  п.13). Применяются **1в1** — движок не пересчитывает и не интерпретирует зону/сторону/note,
  только строит карточку исполнения (лестница 50/30/20, SL за зоной, размер позиции, ликвидации,
  rug-строка). Только лимитные ордера — никакого входа по рынку от лица автора.

## Ночной запуск / watchdog

`scripts/watchdog.sh` — текущий стандарт (ПАКЕТ WATCHDOG, владелец,
2026-07-14; заменяет `scripts/night_run.sh`, который остаётся в репозитории
для истории, но не используется — не запускать оба одновременно). Отличия
от night_run.sh:

1. **Перед стартом** — если в ЭТОМ репозитории (cwd процесса == путь репо,
   проверяется через `lsof -a -p <PID> -d cwd -Fn` по каждому найденному
   `claude`) уже есть живой процесс `claude`, НЕ стартует (защита от гонки
   двух сессий на одном репо), только логирует и выходит. Живые `claude`
   других проектов (другой cwd) игнорируются и не блокируют старт — найдено
   живьём (владелец, 2026-07-14): исходная версия проверяла ВСЕ процессы
   `claude` на машине без учёта cwd, и claude-сессия Академии
   (`dr_linessa_bot`) блокировала старт watchdog для crypto-bot.
2. **Только foreground, без `nohup`** — владелец запускает сам в отдельной
   вкладке терминала.
3. **Перезапуск после падения/выхода claude** — не голый рестарт: пауза
   30с, затем `claude --resume <текущий session-id>` + стартовое сообщение
   «Продолжай с точки обрыва по PROGRESS.md и хвосту git log» (session-id
   берётся из самого свежего транскрипта репозитория в
   `~/.claude/projects/<санитизированный-путь>/*.jsonl` и остаётся тем же
   на все последующие рестарты цикла).
4. Лог перезапусков `logs/watchdog.log`, ротация при >10МБ (одна копия
   `.log.1`) — тот же паттерн, что и в night_run.log.
5. Lock-файл `/tmp/crypto_bot_watchdog.lock` против двойного запуска.
6. Никогда не убивает чужие процессы — только читает `pgrep`/`ps`.

**Запуск**: `~/crypto-bot/scripts/watchdog.sh` — в отдельной вкладке
терминала, БЕЗ `nohup` (foreground).

**Остановка**: `Ctrl+C` в этой же вкладке — рекомендуемый способ: сигнал
уходит сразу на всю foreground-группу процессов (и watchdog, и текущий
`claude`), останавливает мгновенно. `kill $(cat /tmp/crypto_bot_watchdog.lock)`
(TERM) тоже штатно снимает lock и завершает цикл, но проверено живьём на
стенде: пока `claude` активен в foreground, bash откладывает обработку
TERM-трапа до завершения текущего дочернего процесса — то есть `kill` по
PID watchdog'а сам по себе не обрывает текущую активную сессию claude
мгновенно (это стандартное поведение bash при foreground-выполнении
внешней команды, не баг скрипта). Для немедленной остановки прямо во
время активной сессии — `Ctrl+C` в терминале.

**Правило**: `watchdog.sh` запускается **только после завершения**
интерактивной сессии Claude Code в этом репозитории — никогда параллельно
с ней. Два процесса `claude` на одном рабочем дереве одновременно — это
гонки за git/файлы и непредсказуемые коммиты; и lock-файл, и живой
pgrep-чек защищают от этого на старте, но не от интерактивной сессии,
запущенной вручную в обход скрипта, — это ответственность того, кто
запускает.

## Устойчивость к обрывам API (владелец, постоянное правило)

Связь между сессией и владельцем может обрываться в любой момент (сеть, обрыв API) —
работа строится так, чтобы обрыв никогда не терял прогресс и не требовал раскопок при
восстановлении.

1. **Коммит после КАЖДОГО логического шага**, не в конце пакета — функция+тест, wiring
   одного экрана, один найденный и исправленный баг. Обрыв связи не должен оставлять
   больше 15 минут незакоммиченной работы.
2. **Перед каждым таким коммитом** — обновить `PROGRESS.md` одной строкой в формате
   `СЕЙЧАС ДЕЛАЮ: <шаг>, СЛЕДУЮЩИЙ: <шаг>` (поверх обычных содержательных записей
   пакета — это отдельный, всегда актуальный маячок для resume, не замена находкам).
3. **После восстановления связи** (новая сессия/продолжение после обрыва): `git status`
   + хвост `PROGRESS.md` → продолжить с точки обрыва без уточняющих вопросов владельцу
   (сам факт обрыва не требует переспрашивать — прогресс уже виден в файлах).
4. **После КАЖДОГО пуша, затрагивающего Watch Paths (`bot.py` и другой боевой код)**
   — через 3-5 минут проверить, что деплой реально стартовал: сравнить коммит
   контейнера (`railway logs`/deploy-статус) с `origin/main`. Если `SKIPPED` там, где
   ожидался реальный деплой (не журнал/докс-коммит) — не ждать: сразу минимальный
   коммит-триггер, затрагивающий Watch Paths, запушить и повторить проверку. Не
   оставлять контейнер на старом коммите молча.
