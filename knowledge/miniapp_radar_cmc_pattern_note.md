# Дизайн-заметка: паттерны подачи CMC для Mini App Radar v2

Владелец, 2026-07-21: разбор CoinMarketCap как референс ПОДАЧИ (не состава
метрик) для `best_trade_radar_v2.html`. Философия: берём подачу, не набор
данных — глубина конкретного PD-Score радара, не широта агрегатора.
**Приоритет: после диагноза WR, при старте Mini App — не сейчас, только
дизайн.** Код не трогал.

## Сверка с текущим макетом (`best_trade_radar_v2.html`, живой файл)

### 1. Лента топ-виджетов сверху (Market Cap / Fear&Greed / Altcoin Season / Avg RSI)

**Частично уже есть**: макет уже содержит скроллящуюся макро-ленту (`.tape`,
строка 139/243) — BTC/BTC.D/USDT.D/ETH/S&P/DXY/VIX/Ликвидации 24ч. Формат
подходит (компактная строка сверху), но **состав не совпадает** с запросом:

- **Есть в проекте, НЕТ в ленте**: Fear & Greed (`bot.py` — 5.1 в
  `MASTER_ROADMAP.md`, 🟢 живой), Alt Season (упомянут в разведке/
  "Институц." блоке — нужно свериться, насколько это уже живая метрика
  или только упоминание).
- **Market Cap (общий)**: не нашёл отдельной живой метрики в проекте —
  честно, не проверял отдельно, нужна точечная сверка перед тем, как
  проектировать виджет под неё.
- **Avg RSI**: не нашёл агрегированного "средний RSI по рынку" — в
  проекте RSI считается per-символ (`ta_extra.py`), не как рыночный
  индекс. Потребует отдельной агрегации, если делать по паттерну CMC
  буквально.

**Вывод**: не заменять существующую ленту, а ДОБАВИТЬ Fear&Greed и Alt
Season в неё (те, что уже есть данные) — Market Cap/Avg RSI как отдельные
виджеты требуют сначала честной проверки, есть ли под них реальный live-
источник в проекте, не выдумывать заглушку с правдоподобными цифрами.

### 2. BTC.D + USDT.D в шапку (не в скроллящуюся ленту)

**Есть в ленте, НЕ в шапке**: сейчас BTC.D/USDT.D — часть скроллящейся
`.tape`-строки (равноправно с DXY/VIX/ликвидациями), не выделены. Владелец
прав, что это ядро методологии (Блок 8 Cross-asset, `MASTER_ROADMAP.md`:
"8.1 BTC.D/USDT.D Matrix `в бою`") — заслуживает места в `<header>` (строки
142-148 макета), рядом с логотипом/таймштампом, не наравне с второстепенными
макро-тикерами. Дизайн-предложение: статичный (не скроллящийся) блок в
шапке с двумя числами + мини-стрелкой тренда, лента остаётся для остального
макро-контекста.

### 3. 7d-спарклайны рядом со строкой инструмента

**Частично есть, но скрыто**: макет уже рисует спарклайны (`svg.spark`,
колонка "Тренд", строка 198) — НО эта колонка помечена `.procol` (скрыта по
умолчанию, видна только во "Про-режиме", строка 190/259-261). Владелец
хочет их РЯДОМ со строкой инструмента по умолчанию, не за тумблером.
Также честно: макет не специфицирует окно спарклайна явно (может быть
любым) — если нужно именно 7d, это отдельное уточнение при реализации
(какие данные агрегировать: 4h×42 свечи, 1d×7 и т.п.).

**Вывод**: вынести "Тренд"-спарклайн из pro-only колонок в базовый набор
(рядом с "Рост %"), явно зафиксировать окно как 7d при реализации.

## Явно НЕ брать (владелец, подтверждено — вне скоупа этой заметки)

- sentiment-колонка — отложена вместе с Блоком F (роадмап)
- Prediction Markets / Stocks / DTF — вне скоупа продукта
- «Ask AI» — отдельный продукт, не часть Radar v2

Ни один из этих трёх элементов не присутствует в текущем макете — конфликтов
с уже сделанным нет, просто подтверждение, что и не нужно их добавлять.

## Честные открытые вопросы (не решаю, фиксирую для реализации)

1. Market Cap / Avg RSI — нужен ли под них реальный источник в проекте,
   или это второстепенно для v2 первой итерации (можно отложить, оставить
   только Fear&Greed + Alt Season из уже подтверждённых живых данных)?
2. Alt Season — насколько это СЕЙЧАС живая метрика с числом, а не просто
   упомянутая в разведке идея (не проверял отдельно в рамках этой заметки).
3. Спарклайн-окно (7d буквально, или другое) — уточнить при реализации,
   не выдумывать сейчас.

## Статус

Дизайн-заметка, код не менял, `best_trade_radar_v2.html` не трогал.
Приоритет реализации — после диагноза WR, при старте Mini App (владелец).
