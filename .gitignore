best_trade_reader.session*
chat_ids.txt
signal_journal.json
journal/security_log.json
reader_dedup_seen.json
__pycache__/
*.pyc
.DS_Store

# Библиотека знаний SMC (~60ГБ PDF/видео) -- НИКОГДА не коммитить в этот репозиторий.
Trading/
# knowledge/ -- сырые/тяжёлые данные остаются вне git, но курируемые .md-документы
# (KNOWLEDGE_INDEX.md, INVENTORY.md, KNOWLEDGE_GAPS.md, METHODOLOGY_CORE.md,
# SMC_COVERAGE.md) -- реальная документация проекта, версионируем их отдельно ниже.
knowledge/transcripts/
knowledge/channel_archive/
knowledge/_transcribe_worker.log
knowledge/_transcribe_progress.log
knowledge/_transcribe_queue.json
knowledge/_ocr/
knowledge/_ocr_batch.log

# Исторический бэктест (ночная сессия #3) -- сырые свечи (~сотни МБ), не в git.
# Код (download_history.py, engine.py) версионируется, кэш данных -- нет.
backtest/data/
backtest/_progress.log
backtest/_download_stdout.log

# Whale Radar (Блок 1) -- события в стакане/сделках, локальный лог с ротацией,
# не версионируется (тот же принцип, что backtest/data/ -- сырые данные, не код).
data/whale/
# Level Watch (Этап 4 АПГРЕЙД 11.07) -- тот же принцип, что data/whale/ выше.
data/level_watch/
