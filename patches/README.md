# patches/ — топ-5 патчей ночной сессии (готовы, НЕ вмержены)

Снято 2026-07-10, ночная автономная сессия, Блок 4. Каждый патч — реальный, написанный и
протестированный (`py_compile` + смоук + полный `pytest` регрессии — 28/28 без изменений)
код, применённый и подтверждённый локально, затем **отменён** (`git checkout`) — рабочее
дерево осталось чистым, ничего не закоммичено в `bot.py`/`ta_extra.py`. Диф каждого патча
лежит в своей папке (`git apply --check` подтверждено для всех пяти против текущего
`main`).

**Ничего не применять без отдельного «да» владельца по каждому патчу.**

## Как применить один патч (когда будет «да»)

```bash
cd ~/crypto-bot
git apply patches/01-killzone-hours/bot.py.diff
python3 -m py_compile bot.py && python3 -m pytest tests/ -q
git add bot.py && git commit -m "..." && git push
```

## Список (приоритет по `knowledge/MISMATCH_REPORT.md`)

| № | Патч | Файл | Приоритет | Тип изменения |
|---|---|---|---|---|
| 1 | Killzone-часы | `bot.py` | 🔴 | Исправление существующей функции (числа + восстановление стёртой кириллицы) |
| 2 | R:R-гейт 1.5→2.0 | `ta_extra.py` | 🔴 | Изменение боевого порога (торговая логика — требует особой осторожности) |
| 3 | Breaker/Mitigation Block | `ta_extra.py` | 🟡 | Новая аддитивная функция, не подключена никуда |
| 4 | RSI-дивергенция | `ta_extra.py` | 🟡 | Новая аддитивная функция, не подключена никуда |
| 5 | BPR (Balanced Price Range) | `ta_extra.py` | 🟡 | Новая аддитивная функция, не подключена никуда |

Патчи 3-5 **безопасны сами по себе** — это новые функции, которые ничего не вызывает,
поэтому их применение не меняет поведение бота вообще (только добавляет мёртвый код,
готовый к будущему подключению). Патчи 1-2 **меняют поведение уже сегодня** — 1 через
исправление существующей активной функции, 2 через прямое изменение боевого R:R-гейта.
