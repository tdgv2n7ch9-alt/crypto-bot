diff --git a/ta_extra.py b/ta_extra.py
index 5f055e5..5ec0a60 100644
--- a/ta_extra.py
+++ b/ta_extra.py
@@ -42,7 +42,18 @@ ZONE_WIDTH_MAX_PCT = 0.8        # верхняя граница
 ZONE_WIDTH_ATR_MULT = 0.5       # ширина = clamp(ATR% * mult, MIN, MAX)
 ZONE_MIN_TOUCHES = 2            # фрактальная зона валидна только с 2+ касаниями; EMA -- отдельная категория, без порога
 SR_SL_BUFFER_PCT = 2.5          # буфер SL за зоной входа (середина диапазона 2-3% из ТЗ)
-SR_MIN_RR_TP1 = 1.5             # R:R-гейт по TP1
+SR_MIN_RR_TP1 = 2.0             # R:R-гейт по TP1 -- ПАТЧ (ночная сессия, НЕ вмержен без
+                                 # "да" владельца): было 1.5. knowledge/MISMATCH_REPORT.md
+                                 # п.1 -- floor 1:3 у 2 из 4 источников методики (личная
+                                 # методичка владельца + "Санчо"), один источник даёт
+                                 # explicit 1:15 ("Вход в сделку, БУ, RR 1.mp4" [404s]).
+                                 # 2.0 -- намеренно КОНСЕРВАТИВНЫЙ первый шаг (не 3.0, не
+                                 # 1.5->1:3 сразу), т.к. источники методики сами расходятся
+                                 # в 5 раз (1:3 до 1:15) -- нельзя обоснованно взять
+                                 # среднее. См. patches/02-rr-gate/README.md -- ПЕРЕД
+                                 # применением нужен ретроспективный анализ по бакетам R:R
+                                 # на 37 закрытых сделках журнала, это НЕ сделано в рамках
+                                 # этого патча (сам патч -- гипотеза для проверки, не вывод).
 SR_ATR_PERIOD = 14
 
 
