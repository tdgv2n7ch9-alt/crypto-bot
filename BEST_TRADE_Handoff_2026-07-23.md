# BEST TRADE — ХЕНДОФФ ДЛЯ НОВОГО ЧАТА
Срез: четверг 23.07.2026, ~14:55 MSK (UTC+3). Предыдущий: 22.07.2026.
Этот файл = дельта за 23.07 + актуальное состояние.

## 0. РОЛИ (неизменное ядро)
- Этот чат = архитектура/спеки/приёмка/разбор рынка/копируемые блоки.
- Исполнение = Claude Code на Mac mini (~/crypto-bot).
- Решения — за владельцем (Игорь). Русский, мобильный формат, блоки отдельно.
- ГЕЙТЫ (только «да»): промоушены, флаги, деньги, деплой фич, СНЯТИЕ ПАУЗЫ, любое live-изменение.
- Truth Protocol: тест+деплой+live-verify; честное «н/д»; append-only.
- Правый монитор dr_linessa_bot — ЧУЖОЙ проект, НЕ трогать.

## 1. ИНФРАСТРУКТУРА (изменения за 23.07)
- Repo: github.com/tdgv2n7ch9-alt/crypto-bot, main. Деплой только новым коммитом через tools/deploy.sh.
- Railway: worker + **новый публичный домен** `crypto-bot-production-8e57.up.railway.app`
  (создан осознанно, owner-approved, для Mini App). Доступ к данным остаётся owner-only
  (Telegram initData HMAC + chat_id whitelist на всех `/api/v1/*`) — сама HTML-страница
  `/app` публична (без секретов), тот же паттерн что `/api/v1/health`.
- Owner chat ID: 7009350191.

## 2. ГЛАВНЫЙ ИТОГ 23.07: Mini App (Шаг 3/3) + живой инцидент с AUTO-гейтом + CRASHED-деплой

### 2.1 Интерфейс — Шаги 1-3 завершены
- Шаг 1 (card_v2 canon preview) и Шаг 2 (меню/глоссарий/калькулятор) — приняты владельцем
  ранее (22-23.07), с двумя замечаниями (мёртвые кнопки РЫНОК/Зоны/Мои разметки, дедуп
  Исполнения) — обе зафиксированы, обе исправлены (см. 2.4).
- Шаг 3 (Mini App): `miniapp_static/index.html` (owner-only, `/app`) поверх реальных
  `/api/v1/*` (track-record/zones/signals/dashboard). PD-Score конвейер из макета
  `best_trade_radar_v2.html` НЕ реализован как отдельная сигнальная логика — честный
  "не реализовано" вместо выдуманных цифр. `/panel` (owner-only команда) шлёт
  `WebAppInfo`-кнопку — работает, домен создан и подключён (`MINIAPP_PUBLIC_URL`).

### 2.2 ЖИВАЯ НАХОДКА: RSR прошёл AUTO-эмиссию мимо лимита 3/3
- Владелец заметил AUTO-пилот RSR LONG (12:42 MSK) при 3/3 уже занятых слотах
  (US/ETHFI/GMX, 478/479/480 ENTERED).
- Root cause: `_auto_concurrent_limit_reached()` считал `status=="active"` в ЭФЕМЕРНЫХ
  словарях `TOP_LONG_SIGNALS`/`TOP_SHORT_SIGNALS` (персистентны ТОЛЬКО в
  `/tmp/best_trade_signals.json`, без бэкапа в GitHub) — мой же деплой Шага 3 перезапустил
  контейнер, `/tmp` обнулился, счётчик упал 3→0, RSR прошёл эмиссию.
- Фикс (2 итерации, вторая — self-caught баг в первой): счётчик теперь читает
  ПЕРСИСТЕНТНЫЙ `signal_journal._journal` (ENTERED+PENDING по `AUTO_LIVE_SOURCES`),
  scoped по `AUTO_EMISSION_EXPERIMENT_START_TS` (иначе считал вообще ВСЕ AUTO-записи
  когда-либо, 18 против лимита 3 — гейт навсегда заблокирован).
- RSR id=485 отменён (owner-approved прямая правка `journal/signals.json` на GitHub) —
  никогда не входил в позицию (был PENDING), капитал не пострадал.
- Ретро-аудит: AERO(483)/CC(484) — владелец подтвердил визуально (Telegram, 22.07
  вечер, между GMX 16:57 и радаром 22:49 — пусто): алертов НЕ было, оба **корректно
  подавлены гейтом**. Реальная экспозиция подтверждена **3/3**, чисто.

### 2.3 ИНЦИДЕНТ: CRASHED-деплой при попытке задеплоить консолидированный фикс-цикл
- Фикс FIXLIST (add_error_handler + Markdown-retry-обёртка + дедуп кнопок + x100-блэклист
  + косметика) задеплоился как **CRASHED**: `AttributeError: Attribute send_message of
  class ExtBot can't be set!` — `telegram.ext.ExtBot` использует `__slots__`, прямое
  присваивание атрибута ИНСТАНСУ запрещено библиотекой. Тесты не поймали (фейковый класс
  без `__slots__` вёл себя иначе, чем реальная библиотека).
- Простой ~9-10 минут (14:29-14:38 MSK, оценка — Railway logs не хранят историю старого
  контейнера после замены). **Проверка по 1m свечам (Binance/Bybit)**: US/ETHFI/GMX SL/TP
  НЕ касались за окно простоя — исходы не пострадали, честность протокола не нарушена.
- Фикс: патчить КЛАСС (`type(bot_instance)`), не инстанс — `__slots__` не мешает
  переопределить метод на классе. Проверено против РЕАЛЬНОГО `ExtBot` (не только мока)
  ДО повторного деплоя. Redeploy → SUCCESS, 15-минутное чистое окно подтверждено
  (0 новых traceback, AUTO-цикл возобновился штатно).
- **Правило на будущее**: любой monkeypatch чужой библиотеки — smoke-тест против
  РЕАЛЬНОГО класса библиотеки, не только против собственного фейка/мока.

### 2.4 FIXLIST_INTERFACE.md — консолидированный фикс-цикл, ЗАДЕПЛОЕНО, ждёт приёмки
| Пункт | Статус |
|---|---|
| add_error_handler (раньше отсутствовал) | ✅ задеплоено |
| Markdown-retry на уровне класса ExtBot | ✅ задеплоено (после фикса class-vs-instance) |
| РЫНОК/Зоны/Мои разметки — root cause (санитайзер свободного текста) | ✅ задеплоено |
| Дедуп кнопок Исполнения ("СИМВОЛ СТОРОНА · вход X · #N") | ✅ задеплоено |
| x100 блэклист (BANK §22, AKE wash-класс) + футер | ✅ задеплоено |
| x100 противоречие манипуляция→LONG (freshest_sweep_direction) | ✅ задеплоено |
| Косметика "не вошла" / "Закрытые: closed_ts" | ✅ задеплоено |

**Ждёт**: финальная приёмка владельцем тапом по всем кнопкам + `/panel` с телефона.
До этой приёмки — ничего нового не деплоить (стоп по указанию владельца).

## 3. ЭКСПЕРИМЕНТ B — статус без изменений, чисто весь день
- US(478)/ETHFI(479)/GMX(480) — ENTERED, цены входа не искажены, риск 1% каждая,
  ровно 3 позиции (подтверждено дважды: живым аудитом и вердиктом владельца по AERO/CC).
- 0 kill-switch/SL_HIT/TP_HIT/accumulation событий за весь день, включая оба инцидента
  выше (RSR-гейт и CRASHED-деплой) — присмотр не прерывался.

## 4. ОТКРЫТЫЕ ВОПРОСЫ / СЛЕДУЮЩЕЕ
- Финальная приёмка интерфейса владельцем (тап по кнопкам + `/panel`) — блокирует
  дальнейшие деплои интерфейса.
- После приёмки — стандартная очередь: присмотр за экспериментом B (по-прежнему
  приоритет №1) → регрессы → shadow-аналитика → роадмап Фаз B-L.
