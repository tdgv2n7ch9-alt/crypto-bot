"""
Бэктест-каркас (ROADMAP П4 preview) -- ТОЛЬКО чтение уже накопленной истории
signal_journal (real, live outcomes через run_tracker), НЕ фетчит новые исторические
свечи, НЕ трогает боевой сигнальный цикл. "Оживляет" идею мёртвого bot.py:backtest_signal()
честным способом -- прогоняет метрики по РЕАЛЬНО случившимся исходам, а не по
реконструированному заново TP/SL из swing high/low (как делал старый backtest_signal).
"""
