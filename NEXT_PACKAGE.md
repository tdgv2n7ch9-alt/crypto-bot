# NEXT_PACKAGE.md — предложение состава Пакета 10

## Обновление статуса 2026-07-14 (перед утренней сводкой) — что из таблицы ниже уже закрыто

Таблица "Предложение Пакета 13" ниже датирована 2026-07-13 и с тех пор
частично устарела -- честно фиксирую расхождение перед тем, как владелец
будет читать этот файл на приёмке:

- **Пункты 2-4 (EVENT-RADAR М2/М4/М5) -- ЗАКРЫТЫ.** Листинги/делистинги,
  скан новых монет <30 дней, EVENT-DIGEST (пункт 6) -- все три реализованы
  и задеплоены (см. `PROGRESS.md`, разделы "EVENT-RADAR М2/М4/М5").
- **Пункт 7 (EMA-стек/Патч 09 "0 записей") -- частично устарело.** Патч 09
  (OI/funding/L-S) давно превысил порог готовности (n=1136 на
  2026-07-14, порог 100 -- см. ежедневную гигиену в `PROGRESS.md`).
  EMA-стек всё ещё НЕ готов, но уже НЕ "0 записей" -- n=1, копится в
  72-часовом окне (16.7/72ч на момент проверки 2026-07-14). Диагностика
  первопричины низкого темпа накопления EMA-стека остаётся открытым
  вопросом, если владелец хочет её продолжить.
- **Пункты 8-9 (Блок 7а/7б, чтение библиотеки) -- ЗАКРЫТЫ.** `INVENTORY.md`
  синхронизирован (7а), Блок 7б (107 PDF `Trading/`, включая последний
  непокрытый файл `16. Фундаментальный анализ.pdf` 63/63 стр.) дочитан
  2026-07-14 -- см. `EVOLUTION.md`. Видео-транскрипты (159/159 файлов
  расшифрованы) НЕ разобраны с той же построчной строгостью против
  `METHODOLOGY_CORE.md` -- честно остаётся отдельным, не начатым фоном.
- **Пункт 1 (rug/ликвидационная строка в сигнальных карточках) --
  проверено живьём 2026-07-14 (grep по вызовам
  `format_liquidation_cluster_line()` в `bot.py`)**: rug-строка добавлена
  во все алерты отдельным мини-пакетом (см. `PROGRESS.md`), но
  `format_liquidation_cluster_line()` в теле `_build_signal_post()`
  (`bot.py:10254+`, AUTO/точки карточка) НЕ вызывается -- все текущие
  вызовы этой функции (`bot.py:4118,4417,7066`) относятся к другим
  экранам (Разбор/watchlist zone-touch), не к AUTO-карточке. Пункт
  остаётся ОТКРЫТЫМ -- не внедряю сам: требует (а) измерения латентности
  AUTO-цикла с доп. вызовом, (б) явного "да" владельца на изменение
  сигнальной карточки (граница CLAUDE.md).
- **Дополнение "эталон v1.1" (Vol-targeting/ETF Netflow/CFTC COT/
  Corporate treasury/CoinGlass free-tier/Laevitas)** -- остаётся
  НЕ начатым, живая проверка источников владельцем ещё не выполнена
  ни по одному пункту.

**Рекомендация для следующего пакета** (по приоритету, если владелец
согласится): (1) живьём проверить пункт 1 (ликвидационная строка в
AUTO-карточке) -- дёшево, разрешает открытый вопрос; (2) один пункт из
"эталона v1.1" (ETF Netflow через farside.co.uk выглядит самым дешёвым
для живой проверки источника) -- строго проверка источника ДО кода;
(3) если владелец хочет -- разбор видео-транскриптов против
METHODOLOGY_CORE.md тем же уровнем строгости, что PDF Блок 7.

---

## Предложение Пакета 13 (2026-07-13, конец НОЧНОГО ПАКЕТА 12) — остаток EVENT-RADAR + доводка

Составлено по итогам НОЧНОГО ПАКЕТА 12 (полный отчёт — `PROGRESS.md`, записи
"2026-07-13" в конце файла). Пакет SECURITY-HARDENING (7/7 модулей) закрыт
этой же ночью — см. `PROGRESS.md` "ПАКЕТ SECURITY-HARDENING -- ВСЕ 7 МОДУЛЕЙ
ЗАВЕРШЕНЫ". Ниже — что осталось от очереди НОЧНОГО ПАКЕТА 12, честно
нетронутое или частично начатое.

| # | Модуль | Цена | Ценность | Статус |
|---|---|---|---|---|
| 1 | **Строка ликвидационных кластеров в сигнальных карточках** (`send_coin`/`_build_signal_post`) | Низкая-средняя код, но требует измерения латентности AUTO-цикла ДО включения (боевой путь) | Средняя — level_watch-алерты это уже показывают, карточки — нет, несогласованность UX | `format_liquidation_cluster_line()` уже есть в `level_watch.py`, `get_liq_data()` уже кэширует 300с — нужно только (а) измерить время AUTO-цикла с доп. вызовом на 5-10 кандидатах, (б) явное "да" владельца на изменение сигнальной карточки (граница CLAUDE.md) |
| 2 | **EVENT-RADAR М2** (листинги/делистинги через официальные announcement-каналы бирж) | Средняя — RSS/страницы объявлений публичны, не требуют платного API (в отличие от М1/разлоков) | Средняя-высокая — листинг/делистинг двигает цену сильнее многих сигналов | НЕ начато — источники (Binance/Bybit announcement pages) не проверены живьём в этом пакете |
| 3 | **EVENT-RADAR М4** (скан новых монет <30 дней + авто-rug-скор + мемкоин-флаг) | Средняя — `rug_radar.py` уже существует (Пакет 9), нужен только источник "новых листингов CoinGecko" + связка | Средняя — источник потенциальных x100/rug кандидатов | НЕ начато |
| 4 | **EVENT-RADAR М5** (ликвидность сводно — heatmap + DeFiLlama stablecoin flows + USDT.D 30д) | Средняя — DeFiLlama-часть уже используется в Пакете 3 М2 (`onchain_metrics.py`), heatmap уже есть (Пакет 9 М3) — в основном СВОДКА существующих кусков, не новый код с нуля | Средняя | НЕ начато, но большая часть строительных блоков уже есть в проекте — самый дешёвый из оставшихся EVENT-RADAR подмодулей |
| 5 | **EVENT-RADAR М1 (разлоки)** — ЗАКРЫТО решением владельца 2026-07-13 | — | См. `KNOWLEDGE_GAPS.md` "Token unlock calendar" | Владелец выбрал вариант (а) из `UNLOCK_TRACKER_ESTIMATE.md`: собственный трекер НЕ строим (ни узкую, ни полную версию) — 25-30ч ради ~15-20 монет с ретроспективным on-chain даёт плохую цену/ценность, узкая версия создала бы ложное покрытие. Разлоки остаются гэпом, единственный путь при желании вернуться — платный DropsTab Basic $19/мес, решение отдельным шагом в будущем. НЕ блокирует остальные подмодули EVENT-RADAR (М2/М4/М5 — все на бесплатных источниках) |
| 6 | **EVENT-DIGEST в утреннюю сводку/Метрику дня** | Низкая после М2/М4/М5 готовы | Средняя | Зависит от 2-4 выше |
| 7 | **М3/М4 (EMA-стек, Патч 09) — диагностика причины 0 записей** | Средняя (нужна временная инструментация + несколько часов ожидания реальных данных) | Средняя — оба честно "НЕ ЗАКРЫТ"/"без данных" третью ночь подряд без прогресса в диагностике первопричины | НЕ начато в эту ночь (переповерены, статус не изменился с Пакета 11) — рекомендация: следующий пакет должен именно ДИАГНОСТИРОВАТЬ, не просто пересчитывать 0 записей |
| 8 | **Блок 7а: синхронизировать `knowledge/INVENTORY.md` со `KNOWLEDGE_INDEX.md`** (новое, найдено этой ночью) | Средняя — пройтись по ~1350 строкам индекса, проставить реальный статус + alias-имена ("Урок N" = "X.Y.Тема.pdf") в INVENTORY | Высокая — без этого Блок 7б рискует повторно читать уже обработанные файлы (найдено живьём: "Урок 2. Structure..pdf" числился "в очереди", хотя полностью обработан ещё в "Ночь #2" и цитируется в живом shadow-коде `ta_extra.smc_setup_type_body_close_variant()`) | Готов начать сразу, чисто документная задача |
| 9 | **Блок 7б: чтение оставшихся PDF (после 7а)** | Высокая (многочасовая задача) | Низкая-средняя, снижается с каждой ночью откладывания | Ждёт 7а — читать по актуальному, а не устаревшему списку |

Порядок/приоритет — за владельцем на приёмке.

---

## Дополнение 2026-07-13 (эталон v1.1) — кандидаты от владельца, институциональная разведка

Присланы владельцем во время ночной сессии Пакета 11, явно как candidates для
БУДУЩЕГО пакета (не для немедленного внедрения — "живая проверка источника до
кода" ещё предстоит, здесь только зафиксирован состав и источник идеи).
Источник каждого пункта — прямое сообщение владельца в этой сессии (ссылка на
конкретный внешний сервис дана самим владельцем, не мной), запись зеркалирует
в `PROGRESS.md` (раздел "Пакет 12 (EVENT-RADAR) получен").

1. **Vol-targeting sizing** — риск-калькулятор карточек: вариант «институциональный
   сайзинг» (позиция от реализованной волатильности инструмента, ATR/σ14) рядом
   с текущими 1/2/3%. Информационно, формулы риска НЕ менять.
2. **ETF Netflow** (было в бэклоге как 4.10, приоритет поднят) — парсинг
   farside.co.uk, дневные потоки IBIT/FBTC → раздел «Институционал» + фактор
   скоринга shadow. Источник свободный (farside.co.uk) — предстоит живая
   проверка доступности/формата ДО кода, не проверено в рамках этого сообщения.
3. **CFTC COT** (было 4.12) — weekly-парсер CFTC.gov (заявлен владельцем как
   бесплатный API), позиции Large Traders по BTC-фьючерсам CME → Институционал
   + Метрика дня по пятницам. Не проверено живьём в рамках этого сообщения.
4. **Corporate treasury tracker** (было 7.7/7.8) — бесплатные трекеры MSTR/
   корпоративных казначейств, проверить живьём какие есть; недельный дайджест
   «казначейства: куплено/продано».
5. **CoinGlass free-tier** — владелец подтвердил внешне (не мной проверено в
   этом сообщении), что liq maps/OI/funding/L-S доступны бесплатно — сверить
   уже существующий М3-слой (Options Skew + Liquidation Heatmap, Пакет 9) с их
   бесплатным API, расширить покрытие альтов, если лимиты позволяют.
6. **Laevitas MCP** — заметка на будущее, явно НЕ делать сейчас: опционная
   аналитика с MCP-доступом для AI-агентов, кандидат интеграции в Фазе F/ML.
   Зафиксировано в роадмапе, без обязательств по срокам.

Порядок/приоритет между этими 6 пунктами и уже описанным ниже составом
Пакета 10/11/12 — за владельцем на приёмке; здесь только честная фиксация,
без начала работы.

---

Составлено 2026-07-12, конец Пакета 9 (полный отчёт — `PROGRESS.md`, разделы
"Пакет 9 ..." по порядку, плюс калибровка Rug-Radar тем же днём).
**Только предложение — ничего не запущено.** Ждёт приёмки владельца.

Владелец задал состав явно: ядро — сборка `real_full_analysis()` по ТЗ 13
блоков (закрытие Фазы A); бэклог-баг «AUTO-путь слеп к OI/funding/L-S» —
через shadow; миграция `/coin` и `/precision` с deprecated `full_analysis()`.
Ниже — раскладка каждого пункта с ценой/ценностью и порядком, план
слияния уже есть в `ENGINE_UNIFICATION.md` §5 (Пакет 8 М1) — этот пакет его
реализует по шагам, а не изобретает заново.

## Порядок по цене/ценности

| # | Модуль | Цена | Ценность | Готовность к старту |
|---|---|---|---|---|
| 1 | **Ядро: сборка `real_full_analysis()` по 13-блочному ТЗ (Фаза A)** — структурные примитивы (BOS/CHoCH через `ta_extra.smc_setup_type()`, FVG через `ta_extra.find_fvg_zones()`, POI/K-LVL) вместо текущих `%`-пороговых `smc_bos_bull/bear`/`smc_fvg_bull/bear` | Средняя-высокая: `ENGINE_UNIFICATION.md` Шаг 3 — "средний-высокий" риск, обязателен байт-в-байт бэктест ДО/ПОСЛЕ (инфраструктура уже есть, `backtest/`) | **Высокая** — это доминирующий боевой AUTO-путь (30/37 исторических сигналов журнала), сейчас покрывает честно только 4 из 13 блоков ТЗ (`ENGINE_UNIFICATION.md` §4) | Готов начать как **shadow-путь** (параллельный расчёт, не подмена live) — переключение live только отдельным "да" по бэктест-цифрам, тот же протокол, что EMA-стек в Пакете 9 |
| 2 | **Бэклог-баг: OI/funding/L-S слепота AUTO-пути (Блок 7) — через shadow** | Высокая по площади воздействия, но НЕ по сложности кода — сам расчёт уже есть в `fa_engine`/`pro_analysis`, нужно портировать формулу + добавить shadow-поле | **Высокая** — единственный шаг из плана слияния, трогающий одновременно ВСЕ активные боевые пути, если пойдёт в live (`ENGINE_UNIFICATION.md` Шаг 5, "риск: высокий") — именно поэтому владелец явно ограничил это shadow, не live, в этом пакете | Готов начать сразу как отдельный shadow-патч (аналог патчей 01-08 в `shadow_engine.py`) — НЕ зависит от пункта 1, можно параллельно |
| 3 | **Миграция `/coin` и `/precision` с deprecated `full_analysis()`** | Средняя — `full_analysis()` (третий независимый движок) обслуживает только 2 команды, миграция локализована | Средняя — устраняет несогласованность между `/coin`/`/precision` и остальными путями сигналов (`ENGINE_UNIFICATION.md` §3, файл помечен "не мёртв, но не подлежит расширению") | Зависит от исхода пункта 1 — миграция логичнее на УЖЕ обновлённый `real_full_analysis()`/`fa_engine`, не на промежуточное состояние; делать последним |

## Пункт 1 подробнее — как именно "закрыть Фазу A" безопасно

По `ENGINE_UNIFICATION.md` §5 (уже готовый план слияния, Пакет 8 М1),
это Шаг 3 плана. Конкретно:

1. Diff-скрипт (тот же паттерн, что EMA-стек в Пакете 9): для N живых
   символов посчитать `real_full_analysis()`'ы текущие `smc_bos_bull/bear`/
   `smc_fvg_bull/bear` (сейчас `%`-пороговая эвристика) против
   `ta_extra.smc_setup_type()`/`find_fvg_zones()` (честные структурные
   примитивы, уже используются `fa_engine`) — залогировать расхождения с
   разбором ПОЧЕМУ.
2. Backtest-сверка на исторических данных (`backtest/`, инфраструктура уже
   есть с ночи #3) — прогнать оба варианта на одном датасете, сравнить
   win-rate/avg-R/max-DD, как уже делалось для патчей 01/02
   (`PATCH_IMPACT.md`).
3. Shadow-поле в `real_full_analysis()`'ом результате (аналог `ema_stack_
   shadow` из Пакета 9) — накопление на реальном трафике минимум 3-5 суток
   ПЕРЕД любым переключением.
4. Переключение live — отдельное явное "да" владельца по цифрам шагов 2-3,
   НЕ раньше.

## Пункт 2 подробнее — OI/funding/L-S как shadow-патч

Формула уже существует дважды (`fa_engine` Блок 7, `pro_analysis` со своими
порогами) — не пишется с нуля. Задача: выбрать ОДНУ формулу как источник
истины для shadow-версии (рекомендация: `fa_engine`'а вариант — она ближе к
методике курса про funding как доп. фактор перекупленности, см.
`real_full_analysis_TZ_reconstructed.md` Блок 7), добавить как shadow-поле
в `real_full_analysis()`'ый результат (аналог патчей в `shadow_engine.py`)
+ накопление статистики. Live-подача в Rocket Score — НЕ в этом пакете,
отдельное будущее "да" после накопления.

## Пункт 3 подробнее — что именно мигрирует

`bot.py:5343` (`full_analysis()`, обслуживает только `/coin` и `/precision`)
— заменить на вызов уже-обновлённого (после пункта 1) `real_full_analysis()`
или `fa_engine.build_full_analysis()` (решение — какой из двух, за
владельцем: `real_full_analysis()` ближе к тому, что видят подписчики в
AUTO-ленте — согласованность важнее; `fa_engine` полнее по блокам методики
— выбор зависит от того, что владелец хочет видеть в `/coin`). Байт-в-байт
сверка карточек `/coin`/`/precision` ДО/ПОСЛЕ на нескольких символах
обязательна — эти команды видят подписчики напрямую.

## Что НЕ входит в Пакет 10 (сознательно, по границам владельца)

- Live-переключение любого из пунктов 1-2 — только отдельное будущее "да"
  по цифрам, не в рамках этого пакета.
- Шаги 2 (killzone/EMA `pro_analysis()`) и 4 (Order Block в `fa_engine`
  Блок 4) из плана `ENGINE_UNIFICATION.md` §5 — Шаг 2 частично уже сделан
  в Пакете 9 (AMD/killzone кусок 1 live, EMA-стек кусок 2 в shadow, ждёт
  3-суточного окна); Шаг 4 не запрошен владельцем явно для этого пакета,
  можно предложить отдельно после Пакета 10.

---

# ОТЧЁТ ПАКЕТА 9 (закрыт) — см. также PROGRESS.md

Полный текст по каждому пункту — в `PROGRESS.md`, разделы "Пакет 9 ..." по
порядку. Кратко: пункт 4 (перечитка 8 файлов "Урок N") ГОТОВ, серия закрыта
16/19. Пункт 3 (бэклог 3 системных багов) ГОТОВ — задокументирован, не
внедрён. Кусок 2 (EMA-стек diff+shadow) ГОТОВ — диф на 10 символах, shadow
задеплоен, live НЕ переключён, ждёт 3-суточного окна. Модуль Rug-Radar
ГОТОВ — кейс LAB в METHODOLOGY_CORE.md §21, `rug_radar.py` с 5 детекторами,
подключён в x100/Памп-радар информационно. М4 (Etherscan whale-tracking)
ГОТОВ — `etherscan_whale.py`, ждёт регистрации ключа владельцем. М3
(Options Skew + Liquidation Heatmap) ГОТОВ, в карточке "Обзор". М5
(`/patterns` дайджест) ГОТОВ. М6 (библиотека) ГОТОВ — 4-й независимый
источник прочитан, честно без новых правил. Отдельно тем же днём —
калибровка `exchange_transfers` Rug-Radar по прямому "да" владельца:
эталонный тест LAB пройден (20→45, пересекает WARN), 3 здоровых токена без
ложных срабатываний. Все живые деплои подтверждены `railway run`. 548
passed, 1 skipped, без регрессий на конец пакета.

Стоп до приёмки владельца.

---

## НОЧЬ#3, Н7 (владелец, факт без подключения) — level_watch НЕ видит journal/spot_plans.json

Проверено (`grep -n "spot_plans" level_watch.py bot.py` → 0 совпадений; `grep -rln
"spot_plans" --include="*.py" .` → только `tools/summer_spot_plan.py` и его тест):
`level_watch.py` читает ИСКЛЮЧИТЕЛЬНО `journal/watch_zones.json`
(`WATCH_ZONES_FILE`, `level_watch.py:55`) — про существование
`journal/spot_plans.json` (Пакет 16, "летний спот-план", пишется
`tools/summer_spot_plan.py`, 1443 байта на момент проверки, 8 символов с
tier/zone/ladder/sl/invalidation/note) в коде `level_watch.py` нет ни единого
упоминания. Файл существует на диске и обновляется отдельным инструментом, но ни
one из зон spot_plans.json не участвует в zone-touch алертах/лестнице ⭐ ЛИМИТКИ —
это два параллельных, не связанных источника разметки.

Честная структурная заметка (не решение, только наблюдение): схема записи в
`spot_plans.json` (tier/zone/ladder/sl/invalidation/note) почти 1:1 совпадает по
форме с author-зонами `watch_zones.json` (tier/zone/side/prio/note, см. Пакет 18
п.13) — если владелец захочет когда-нибудь объединить два источника разметки в
одну ленту, структурной переделки формата не потребуется, только код-мост. НЕ
подключено по прямому поручению ("НЕ подключать — только факт").
