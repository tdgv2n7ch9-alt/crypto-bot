# SECRETS_AUDIT.md — санитарный аудит секретов

Просканировано 2026-07-10: рабочее дерево (`main`, все файлы кроме `Trading/`/`knowledge/_ocr`,
которые проверялись отдельно при чтении библиотеки), вся git-история (`git log --all -p`,
обе ветки `main` и `local-backup`), поиск по паттернам `ghp_`/`github_pat_`/Telegram bot
token (`\d{9,10}:[A-Za-z0-9_-]{35}`)/`TG_API_HASH`/`TG_API_ID`/generic
`password=`/`secret=`. **История НЕ переписывалась** — только отчёт, ротация/чистка
истории — решение владельца.

Значения ниже частично замаскированы (первые+последние символы) — этого достаточно, чтобы
владелец опознал, какой именно секрет, без ещё одного полного воспроизведения в файле.

---

## 1. 🔴 САМОЕ СРОЧНОЕ — CMC_API_KEY хардкожен в ТЕКУЩЕМ, ЖИВОМ bot.py

- **Файл**: `bot.py:117` (текущий HEAD, `main`, задеплоено на Railway прямо сейчас)
- **Что**: `CMC_API_KEY = os.getenv("CMC_API_KEY", "7c58...b53a")` — реальный ключ
  CoinMarketCap используется как **дефолтное значение**, если переменная окружения не
  задана. Это не история — это код, который выполняется в проде СЕЙЧАС.
- **Риск**: любой, у кого есть доступ к приватному репозиторию (или к будущему форку/копии
  кода), видит рабочий CMC-ключ в открытом виде, без необходимости смотреть Railway env vars.
- **Рекомендация**: убрать хардкод-дефолт (`os.getenv("CMC_API_KEY")` без второго
  аргумента, как уже сделано для `BOT_TOKEN` на той же странице) — это чисто инфраструктурная
  правка (не трогает торговую логику), но по правилам сессии я её не вношу без отдельного
  явного "да" (правки в `bot.py` уже делались много раз за ночь, эта — не про сигналы, но
  всё равно спрашиваю, т.к. это меняет поведение при отсутствии env var: бот перестанет
  тихо работать на дефолтном ключе, если `CMC_API_KEY` не задан на новом деплое — это разница
  в поведении, а не только косметика). Отдельно — сам ключ стоит считать потенциально
  просвеченным и обдумать ротацию на стороне CoinMarketCap.

**✅ ЗАКРЫТО 2026-07-10 (в тот же день):**
1. Хардкод-дефолт убран из `bot.py:117` (владелец дал явное "да") — `os.getenv("CMC_API_KEY")`
   без второго аргумента, `_validate_cmc_key()` заодно защищён от падения на `None`.
2. Владелец сделал живой запрос к `pro-api.coinmarketcap.com/v1/key/info` со старым ключом —
   **1001 "This API Key is invalid"**, ключ подтверждённо мёртв. Ротация на стороне CMC и
   обновление Railway — сделал владелец сам.
3. Найден и закрыт ещё один источник экспозиции того же ключа, не отмеченный в находке
   выше на момент записи — `~/.zshenv` (локальный, вне git, грузится в каждый shell)
   содержал `export CMC_API_KEY=<тот же ключ>`. Строка удалена (диф показан и подтверждён
   владельцем). В том же файле остаются живые `BOT_TOKEN`/`TG_API_ID`/`TG_API_HASH` — не
   трогал (не просили, штатный локальный конфиг, не приближаюсь без отдельного запроса).

## 2. 🔴 GitHub PAT в открытом виде — 2 разных места на диске (не в git)

- **Место A**: `Trading/BEST TRADE Handoff.pdf` (найдено ранее в этой сессии, уже
  зафиксировано отдельной записью в PROGRESS.md/задачей #30) — `ghp_BCzL...O5hQ`, помечен
  "действующий на момент записи" (01.07.2026).
- **Место B** (новая находка этого прохода): `.claude/settings.local.json:16` — тот же
  токен `ghp_BCzL...O5hQ` в логе разрешённых bash-команд (история одобренных `curl`-вызовов
  к GitHub API). Этот файл **глобально гитигнорится** (`~/.config/git/ignore`), в git
  никогда не попадал (`git log` для файла пуст) — риск локальный (на этом Mac mini), не
  через GitHub.
- **Риск**: если это текущий `GITHUB_TOKEN`, используемый ботом в Railway для
  Contents API (подписчики/журнал/бэкапы) — совпадает с тем, что уже отмечено в задаче #30.
- **Рекомендация**: одна и та же, что уже дана раньше — проверить, используется ли этот
  токен сейчас как `GITHUB_TOKEN` в Railway; если да и есть малейшее сомнение в его
  безопасности — ротировать через GitHub Settings → Developer settings → Personal access
  tokens. Файл `.claude/settings.local.json` можно почистить вручную (не мой файл для
  автоматической правки без запроса).

**✅ Живая проверка 2026-07-10** (по аналогии с CMC-ключом, по явному "да" владельца):
`GET https://api.github.com/rate_limit` с этим токеном → **`401 Bad credentials`** — токен
**мёртв**, уже недействителен. Срочность ротации снята (рабочий `GITHUB_TOKEN` бота в
Railway отдельный, не завязан на этот PAT). Ротация на GitHub всё ещё правильная гигиена
(отозвать явно, а не полагаться на то, что он "просто перестал работать"), но не аварийная.
Честно: сам этот проверочный запрос добавил токен ещё в одно место — JSONL-транскрипт
текущей сессии (`~/.claude/projects/.../*.jsonl`) — неизбежно для живой проверки, но
фиксирую для полноты списка мест экспозиции. Место B (`.claude/settings.local.json`) уже
почищено ранее в этой же сессии.

## 3. 🟡 Telegram BOT_TOKEN хардкожен в СТАРОЙ git-истории (не в текущем HEAD)

- **Коммиты**: `9e0701d` ("Update bot.py"), `60a6801` ("Add files via upload") — оба очень
  ранние, есть в истории `main` (через `origin`) и в `local-backup`.
- **Что**: `BOT_TOKEN = "8840...ANMA"` (и опечаточный вариант `8840...ANMA` с другим
  написанием части символов в одном из коммитов — похоже на два разных сохранения одного
  редактирования, не два разных токена, не проверял досконально).
- **Текущий HEAD чист**: `bot.py:116` сейчас — `BOT_TOKEN = os.getenv("BOT_TOKEN")`, без
  хардкод-дефолта. Проблема только в истории, которая логически "живёт вечно" в приватном
  репозитории, пока её не переписать (что я делать не буду без отдельного явного запроса —
  переписывание истории может сломать `local-backup`/любые другие ссылки).
- **Важное отличие от текущего**: числовой префикс токена (`8840157382`, это Telegram ID
  бота) НЕ совпадает с тем, что видно в текущем окружении (`BOT_TOKEN` в этой сессии
  начинается на другую цифровую последовательность — не буду печатать полностью ни ту, ни
  другую, но префиксы разные) — **похоже на токен другого/более старого экземпляра бота**,
  не обязательно текущий активный `@bt_crypto_bot`. Не факт, что всё ещё валиден — не
  проверял (не буду дёргать Telegram API с найденным в истории токеном без разрешения).
- **Рекомендация**: если это токен старого/заброшенного бота — низкий приоритет, но по-хорошему
  стоит зайти в @BotFather и убедиться, что такого токена/бота не осталось активным
  где-то ещё. Если сомневаетесь, что это старый бот — сначала проверьте сами (не просите
  меня дёргать API этим токеном).

## 0. 🔴 ИСПРАВЛЕНИЕ пункта 4 ниже -- TG_API_HASH БЫЛ хардкожен, и это ЖИВОЙ, ТЕКУЩИЙ ключ

**2026-07-13, Пакет SECURITY-HARDENING М2, найдено `gitleaks git --log-opts="--all"`**
(владелец явно попросил инструментальный скан вместо ручного grep, которым делался
предыдущий проход этого документа): пункт 4 ниже **был неверен** -- ручной grep пропустил
эту находку, честно исправляю, а не молчу.

- **Коммит**: `caac26a194402394d912b837d81d3b89d687361b` ("Add files via upload"),
  2026-06-23, файл `reader.py:5` -- `API_HASH="<значение>"` хардкожен напрямую (не как
  default у `os.getenv`, а буквально как значение переменной).
- **Текущий HEAD чист**: `reader.py:29` сейчас -- `API_HASH = os.getenv("TG_API_HASH", "")`,
  без хардкода. Проблема только в истории.
- **КРИТИЧНО, проверено живьём БЕЗ печати самого значения** (SHA256-сравнение, не текст):
  `sha256(значение_из_коммита) == sha256(текущий TG_API_HASH в Railway)`.
  **Это ТОТ ЖЕ, СЕЙЧАС ДЕЙСТВУЮЩИЙ ключ Telethon-приложения** (my.telegram.org), не старый
  заброшенный -- в отличие от находки BOT_TOKEN в пункте 3 (там числовой префикс не совпал
  с текущим). Любой, у кого был доступ к приватному репозиторию в момент коммита
  `caac26a` (2026-06-23) и позже, видит рабочий `api_hash` в открытом виде.
- **Риск**: `api_id`/`api_hash` сами по себе НЕ дают доступ к аккаунту/сессии (для этого
  нужен ещё и файл сессии или интерактивный вход), но позволяют создать СВОЁ приложение
  под тем же `api_id`, что может привести к общим квотам/ограничениям Telegram или, в
  худшем случае, к бану пары `api_id`/`api_hash`, если кто-то злоупотребит ей под видом
  этого приложения.
- **Рекомендация -- ТРЕБУЕТ владельца**: как и с Telethon-реавторизацией (`RESTORE_GUIDE.md`),
  ротация `api_hash` делается через my.telegram.org (My Applications → выбрать приложение
  → возможно потребуется пересоздать приложение целиком, т.к. Telegram не всегда даёт
  перевыпустить `api_hash` для существующего app_id без пересоздания) -- интерактивный шаг,
  агент выполнить не может. После ротации: обновить `TG_API_HASH` (и, если пересоздано
  приложение, `TG_API_ID`) в Railway Variables, затем реавторизовать Telethon-сессию
  (тот же шаг, что уже требуется по независимой причине -- `is_user_authorized()=False`).

## 4. 🟢 Проверено и НЕ найдено (честно, не значит "не может быть") -- ОБНОВЛЕНО

- ~~`TG_API_HASH`/`TG_API_ID` (Telethon) — ни разу не хардкожены нигде в истории обеих
  веток~~ -- **ОШИБКА предыдущего ручного прохода, исправлена в пункте 0 выше.**
  `TG_API_ID` отдельно НЕ найден хардкоженным нигде в отчёте gitleaks (только `TG_API_HASH`).
- `.env`-файлы — ни разу не коммитились (`git log --diff-filter=A --name-only` по всей
  истории на `\.env` — пусто).
- Generic `password=`/`secret=` с реальными значениями — не найдено (подтверждено также
  инструментально `gitleaks`, не только ручным grep).
- Ключи бирж (Bybit/Binance) — **не искал специально** (граница "не трогать ключи API
  бирж" — не расширяю до "не читать/не искать", но осознанно не делал точечный
  углублённый грep по этим конкретным именам переменных, чтобы не приближаться к границе
  без явного запроса; если нужно — отдельная просьба). `gitleaks` тоже не нашёл ничего
  под этим именем автоматически (правило `generic-api-key` сработало бы, если бы такой
  ключ был закоммичен в явном виде) -- честно: это не полная гарантия отсутствия (низкая
  энтропия/нестандартный формат ключа биржи мог не сработать на правило), не выдаю за
  100%-подтверждение.
- Полный JSON-отчёт `gitleaks` (1549 коммитов просканировано, 3 находки всего -- все три
  разобраны выше в пунктах 0/1) сохранён в session-scratchpad (НЕ в git, НЕ в `/tmp`
  напрямую -- содержит секреты в явном виде, даже если исторические; путь доступен в
  сессии, не публикую здесь).

## Как читать этот документ
Пункт 0 (TG_API_HASH, живой) и пункт 1 (CMC_API_KEY, уже закрыт) -- то, что было
критично. Пункты 2-3 -- реальные, но менее срочные (локальный файл вне git / история
старого BOT_TOKEN, вероятно неактивного). Решение по каждому — ротировать/не ротировать/
чистить/не чистить — за владельцем, я только нашёл и задокументировал, ничего не менял
и не переписывал историю.

## 5. Пакет SECURITY-HARDENING М2 -- ротация BOT_TOKEN, шаги для владельца

Владелец явно запросил ротацию `BOT_TOKEN` как часть "token hygiene", независимо от
находок выше (это профилактика, не реакция на конкретную утечку живого токена -- живой
`BOT_TOKEN` НЕ найден скомпрометированным этим аудитом, найденный в истории токен
похож на другой/старый экземпляр бота, см. §3). Интерактивный шаг, агент выполнить не
может (нужен диалог с @BotFather в Telegram от лица владельца) -- шаги для владельца:

1. Открыть диалог с **@BotFather** в Telegram (тот аккаунт, под которым изначально
   создавался `@bt_crypto_bot` или актуальный бот).
2. `/mybots` → выбрать бота → **API Token** → **Revoke current token** -- это сразу
   инвалидирует старый токен и выдаёт новый.
3. Скопировать новый токен, зайти в Railway → проект `resilient-respect` → сервис
   `crypto-bot` → Variables → обновить `BOT_TOKEN` новым значением → deploy применится
   автоматически (Railway рестартует процесс на новой переменной).
4. Сообщить мне (в следующей сессии/сообщении), что готово -- я живьём проверю:
   (а) что бот отвечает на команды с новым токеном (через `railway logs` -- увижу
   успешный polling к `api.telegram.org`), (б) что старый токен деактивирован (прямой
   запрос к Telegram Bot API старым токеном должен вернуть `401 Unauthorized` --
   ЕСЛИ владелец сам предоставит старый токен для этой конкретной разовой проверки;
   я не храню и не могу воспроизвести старый токен сам).

**Честно**: я не могу инициировать шаги 1-3 сам -- Telegram не даёт ботам/API управлять
токенами других ботов программно, только через интерфейс BotFather от владельца
аккаунта. Это тот же класс блокера, что Telethon-реавторизация Reader (`RESTORE_GUIDE.md`)
и ротация `TG_API_HASH` (§0 выше) -- все три требуют владельца, я готовлю и проверяю,
не выполняю интерактивную часть сам.
