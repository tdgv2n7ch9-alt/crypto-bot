# RETRO_WR_DIAGNOSIS.md — ретро-диагноз WR по закрытым сделкам (append-only)

Владелец, 2026-07-21, приоритет №1. **READ-ONLY** — код/гейты/флаги не менял,
только анализ уже накопленных shadow+journal данных. Каждая запись — снимок
на момент прогона, не пересчитывается задним числом.

---

## Снимок 2026-07-21 ~16:10 MSK

### ШАГ 0 — гейт чистоты

Подтверждено ДО анализа (`railway ssh`, живой контейнер, деплой
`ada2a9cf`): `integrity_report()` на активный+архив = **`duplicate_count: 0,
out_of_order_count: 0`**, архив 802/802 синкнут. Гейт зелёный — анализ
разрешён.

### Честная находка ДО начала анализа: расхождение n=15 vs n=23 vs n=22

Инструкция владельца ссылалась на n=15 (база 20%) и отдельно на n=23 (база
17.4%, 4/23) — оба числа уже устарели относительно момента фактического
прогона. Прямой вызов `shadow_outcome_analysis.closed_outcomes_report()`
на свежем снимке дал **n=22, WR=18.2% (4/22)** — на 1 сделку меньше, чем
n=23 несколькими минутами ранее (символ `DEXE`, `journal_id=471`, статус
`SL_HIT`, но соответствующая shadow-запись имеет `promoted_live: None`, не
`True`/`False` — официальная функция дедупа/фильтра честно её исключает,
такое же исключение делает и `_dedup_by_trade()` при ручном
воспроизведении). Это не ошибка подсчёта — система живая, база меняется в
реальном времени между двумя последовательными вызовами `railway ssh` в
пределах нескольких минут.

**Решение (владелец, финально)**: основной анализ — **n=23 по факту
инструкции** (использую фактически подтверждённые n=22 как ближайший
воспроизводимый эквивалент — расхождение в 1 сделку из-за живой гонки
данных, не меняет ни один вывод ниже). Базовая WR для корреляций и
трендового блока = **18.2% (4/22)**, с отдельным честным трендовым блоком
по составу.

---

## ШАГ 1 — выборка

22 уникальные закрытые сделки (`_dedup_by_trade()` по `journal_id`,
последний снимок на сделку), контур `live`. Список (символ, направление,
исход, `actual_r`):

| # | Символ | Направление | Исход | R |
|---|---|---|---|---|
| 1 | BEAT | short | SL_HIT | -1.00 |
| 2 | US | long | TP1_HIT | +3.27 |
| 3 | US | long | SL_HIT | -1.00 |
| 4 | INJ | long | TP1_HIT | +1.59 |
| 5 | INJ | long | SL_HIT | -1.00 |
| 6 | GIGGLE | long | SL_HIT | -1.00 |
| 7 | TIA | long | SL_HIT | -1.00 |
| 8 | BTW | short | SL_HIT | -1.00 |
| 9 | APR | long | SL_HIT | -1.00 |
| 10 | 2Z | long | SL_HIT | -1.00 |
| 11 | SYRUP | long | SL_HIT | -1.00 |
| 12 | KAITO | long | TP1_HIT | +1.65 |
| 13 | TRUMP | short | SL_HIT | -1.00 |
| 14 | APR | long | SL_HIT | -1.00 |
| 15 | T | long | SL_HIT | -1.00 |
| 16 | NIGHT | short | TP1_HIT | +2.06 |
| 17 | ZEC | long | SL_HIT | -1.00 |
| 18 | AKT | short | SL_HIT | -1.00 |
| 19 | US | long | SL_HIT | -1.00 |
| 20 | CFG | short | SL_HIT | -1.00 |
| 21 | UB | long | SL_HIT | -1.00 |
| 22 | VVV | long | SL_HIT | -1.00 |

**База: 4 win / 22 = 18.2%, PF (по `actual_r`) считается ниже по группам.**

---

## ТРЕНДОВЫЙ БЛОК — база дрейфует вниз, не стабильна

| Срез | n | WR | PF |
|---|---|---|---|
| Старые 15 (#1-15) | 15 | **20.0%** (3/15) | 0.54 |
| Новые 7 (#16-22) | 7 | **14.3%** (1/7) | 0.34 |

Владелец ожидал 12.5% (1/8) на новых — расхождение в 1 сделку (7 против 8)
из-за того же расхождения n=22 vs n=23 (`DEXE` не входит в мой
воспроизводимый снимок, см. выше). Направление вывода **то же**: свежие
сделки хуже старых, база НЕ стабильна, дрейфует вниз. PF на новых (0.34)
хуже, чем на старых (0.54) — тоже хуже, не только WR.

### Честная находка №1 (важнее любой корреляции ниже): shadow-данные пусты у ВСЕХ 6 новых убытков

Из 7 новых сделок — 1 выигрыш (NIGHT, полный набор shadow-полей) и 6
убытков (ZEC, AKT, US#2, CFG, UB, VVV). **Все 6 убытков имеют ПОЛНОСТЬЮ
пустые (`None`, не `False`) все shadow-аналитические поля**:
`amd_phase_methodology`, `tz13_score`, `tz13_setup_type`,
`inducement_swept`, `whale_klvl_confluence`, `bpr_confluence`,
`shadow_rr_gate_pass`, `oi_funding_ls_shadow` — ни одно не посчиталось
вообще, не просто "не сработало" (`False`), а буквально не вычислено.
Диапазон дат этих 6 сделок — 2026-07-18 15:18 по 2026-07-21 06:57 (почти
3 суток), не короткий инцидент.

**Честно, не выдумываю причину**: не смог однозначно подтвердить root
cause логами в рамках этого прогона (см. `log_send_scheduled_shadow_async`
докстринг в `shadow_engine.py` -- try/except вокруг `compute_shadow()`
ловит ошибки ЦЕЛИКОМ, но здесь запись УСПЕШНО записалась с частичными
None-полями, то есть сбой скорее ВНУТРИ отдельных под-вычислений
`compute_shadow()`, каждое из которых, вероятно, честно возвращает None
при недостатке данных/ошибке -- не проверял на предмет "мало 4h-истории у
новых токенов" (ZEC/AKT/CFG/UB/VVV — не выглядят как совсем свежие
листинги, но не проверял точную дату листинга ни одного). **Это отдельная,
самостоятельная находка регресса/пробела, не входит в объём этого
read-only анализа — рекомендую отдельную задачу расследования** (приоритет
"а" по постоянному контуру CLAUDE.md — регресс, а не просто наблюдение).

Практическое следствие для ЭТОГО диагноза: корреляционный анализ ниже
считается на n=16 (сделки С shadow-данными: 15 старых + NIGHT), НЕ на
полных 22 — честно уже, чем формальные "22", но иначе анализ считал бы
"отсутствие данных" как признак "False", что было бы неправдой.

---

## ШАГ 3 — корреляции по признакам (n=16 с shadow-данными, база WR=25.0%, PF=0.71 на этом подмножестве)

| Признак | Значение | WR | PF | n |
|---|---|---|---|---|
| **amd_phase** | manipulation_*/dead_zone | **66.7%** | **4.29** | 6 |
| **amd_phase** | accumulation/distribution_bull | **0.0%** | **0.00** | 9 |
| tz13_setup_type | AMD | 50.0% | 1.83 | 4 |
| tz13_setup_type | None (не классифиц.) | 25.0% | 0.82 | 8 |
| tz13_setup_type | Sweep | 0.0% | 0.00 | 4 |
| inducement_swept | True | **37.5%** | **1.38** | 8 |
| inducement_swept | False | 14.3% | 0.27 | 7 |
| shadow_rr_gate_pass | True | 25.0% | 0.89 | 8 |
| shadow_rr_gate_pass | False | 25.0% | 0.54 | 8 |
| breaker_mitigation | breaker | 25.0% | 1.09 | 4 |
| breaker_mitigation | mitigation | 20.0% | 0.41 | 10 |
| oi_funding_ls_shadow | положительный | 25.0% | 0.62 | 8 |
| oi_funding_ls_shadow | отрицательный | 25.0% | 0.53 | 4 |
| grade (весь n=22) | A | н/д (n=1) | н/д | 1 |
| grade (весь n=22) | B | 20.0% | 0.54 | 20 |
| direction (весь n=22) | long | 18.75% | 0.50 | 16 |
| direction (весь n=22) | short | 16.7% | 0.41 | 6 |
| divergence против направления | True | 50.0% (n=2, **честное н/д — n<5**) | 2.06 | 2 |
| bpr_confluence | True | 0.0% (n=1, **честное н/д**) | 0.00 | 1 |
| bpr_confluence | False | 26.7% | 0.78 | 15 |

### Мёртвые фильтры (не варьируются вообще)

- **`whale_klvl_confluence`** — `False` во ВСЕХ 16 сделках с данными, ни
  разу `True`. Подтверждённый мёртвый фильтр (тот же класс, что владелец
  предполагал). Кандидат на отдельную починку/расследование, не на вывод
  из этого диагноза.

### Ранжированный список кандидатов (по lift WR, честно только там где n>=4)

1. **AMD-фаза `manipulation_*`/`dead_zone` vs `accumulation`/`distribution`**
   — lift **+66.7пп** (66.7% vs 0.0%), PF 4.29 vs 0.00, n=6 vs n=9. Самый
   сильный кандидат, подтверждён УЖЕ ТРЕТИЙ раз за сессию на растущих n
   (n=15→17→сейчас n=16 с shadow-данными) — согласуется с базовой AMD-
   методологией (не входить до стоп-ханта).
2. **`inducement_swept=True`** — lift +23.2пп (37.5% vs 14.3%), PF 1.38 vs
   0.27, n=8 vs n=7. Владелец ожидал ~50% — получено 37.5%, ниже ожидания,
   но направление верное и лифт реальный.
3. **`tz13_setup_type=AMD`** — WR 50%, PF 1.83, n=4 (частично перекрывается
   с находкой №1, так как AMD-фаза и AMD-сетап коррелируют по конструкции).
4. **`tz13_setup_type=Sweep`** — АНТИ-сигнал, WR 0%, PF 0.00, n=4 — все 4
   сделки этого типа закрылись по SL. Маленький n, но 100%-й результат в
   одну сторону — стоит отдельного внимания.

**Честно НЕ кандидаты (нет лифта или n слишком мал)**: `shadow_rr_gate_pass`
(25% в обеих группах — гейт не различает), `breaker_mitigation` (25% vs
20%, разница внутри шума на этом n), `oi_funding_ls_shadow` знак (25% в
обеих группах), `grade`/`direction` (нет содержательной разницы),
`divergence`/`bpr_confluence` (n<5, честное н/д).

---

## ШАГ 4 — MFE/MAE разбор (n=8 сделок с данными: TRUMP, NIGHT, ZEC, AKT, US#2, CFG, UB, VVV)

MFE/MAE в R посчитаны по факту (`entry_mid`, `sl`, `mfe_price`/`mae_price`
из journal). **Честно: только 8 из 22 сделок имеют это поле** (добавлено
в трекер позже, старые записи 07-12..07-17 поля не имеют — н/д для них,
не выдумываю).

| Символ | Исход | MFE (R) | MAE (R) | Класс |
|---|---|---|---|---|
| TRUMP | SL_HIT | **-0.52** | +1.06 | (B) вход не сработал -- даже в плюс не сходил |
| ZEC | SL_HIT | +0.67 | +1.00 | **промежуточный** (0.3-1.0R) -- ни А, ни B чисто |
| AKT | SL_HIT | +1.59 | +1.34 | (A) вход был прав, управление/SL |
| US(#2) | SL_HIT | +1.40 | +1.01 | (A) вход был прав, управление/SL |
| CFG | SL_HIT | +0.17 | +1.04 | (B) вход не сработал |
| UB | SL_HIT | **+3.02** | +1.06 | (A) вход был прав -- ушёл в +3R, потом развернулся до SL |
| VVV | SL_HIT | +0.28 | +1.13 | (B) вход не сработал (граница) |
| NIGHT | TP1_HIT | +2.44 | -0.62 | (выигрыш, не классифицируется) |

**Из 7 убытков с MFE/MAE-данными**: 3 (A) — AKT, US, UB (43%); 3 (B) —
TRUMP, CFG, VVV (43%); 1 промежуточный (ZEC, 14%) — честно не форсирую
его ни в одну группу (0.67R -- между порогами владельца 0.3/1.0).

**Вердикт A vs B**: **ПОЧТИ РОВНЫЙ СПЛИТ, не одна доминирующая причина**
на этом n=7. Не могу честно сказать "проблема входа" ИЛИ "проблема
управления" — судя по этой малой выборке, ОБЕ причины присутствуют
примерно поровну. `UB` (+3.02R до разворота в SL) — самый яркий отдельный
случай в пользу правила "перевод в безубыток/сокращение риска после
+1R", но это ОДИН случай, не статистика.

**Честная оговорка**: n=7 — категорически мало для уверенного вывода в
любую сторону (владелец сам просил "честное н/д где n<5" — здесь n=7,
формально выше порога, но разница 3 vs 3 внутри шума на таком n).

---

## Итоговый список кандидатов владельцу (НЕ автопромоушен, только материал для решения)

1. **AMD-фаза (manipulation/dead_zone vs accumulation/distribution) как
   входной фильтр** — самый сильный и наиболее воспроизводимый кандидат
   за всю сессию (n=6 vs n=9, WR 66.7% vs 0%). Рекомендация: продолжать
   копить в shadow до n≥20 в "хорошей" группе, затем отдельный вопрос
   владельцу.
2. **`inducement_swept` как входной фильтр** — лифт +23.2пп, ниже
   ожидания владельца (~50%), но направление верное. Продолжать копить.
3. **`tz13_setup_type=Sweep` как анти-сигнал** — 0% на n=4, маленькая
   выборка, но 100%-й результат в одну сторону, стоит отдельного
   наблюдения.
4. **`whale_klvl_confluence` — подтверждённый мёртвый фильтр**, кандидат
   на отдельную задачу расследования (почему никогда не True), НЕ на
   вывод из этого анализа.
5. **Разрыв shadow-данных для 6 из 7 последних сделок (07-18..07-21)** —
   ОТДЕЛЬНЫЙ, возможно более срочный регресс, требующий расследования вне
   рамок этого read-only диагноза. Пока не расследовано — ограничивает
   достоверность любого будущего анализа на растущем n, если не починить.
6. **MFE/MAE A/B вердикт — неопределённый на этом n** (3 вход-ОК/3
   вход-не-сработал/1 промежуточный), не даёт основания выбрать между
   "чинить вход" и "чинить управление" однозначно — нужно больше данных
   (продолжать копить mfe_price/mae_price, они пишутся только с недавнего
   времени).

**Ничего из вышеперечисленного не применено к боевому чек-листу/гейтам —
только кандидаты, решение за владельцем.**

---

# СНИМОК 2: CORRECTED MATCHING (2026-07-21, владелец, read-only)

Причина: `SHADOW_REGRESSION_INVESTIGATION.md` (2026-07-21) установил, что
"разрыв shadow-данных для 6 сделок" из Снимка 1 (пункт 5 итогового списка
выше) был артефактом БИТОГО МАТЧИНГА в скрипте-экстракторе (шёл по
ближайшему `ts` без фильтра, цеплял хвостовую `auto_onchain_shadow`-запись
без аналитических полей вместо правильной `source="send_scheduled"`-записи),
не регрессом пайплайна. Этот снимок — полный пересчёт с исправленным
матчингом. Методология подсчёта WR/PF идентична Снимку 1 (не менялась).

## Метод матчинга (исправленный)

1. Фильтр по `source == "send_scheduled"` — исключает 4 параллельных
   Фаза-B auto_*_shadow типа (derivatives/options/liquidation/onchain),
   у которых нет amd/tz13/inducement/patch05/09-полей в принципе.
2. Матчинг shadow→журнал: `live_journal_id` напрямую (для записей ПОСЛЕ
   2026-07-12 10:11, Пакет 7 М2), иначе time-window по (symbol, direction,
   ±10 мин) — та же логика, что `shadow_outcome_analysis.match_shadow_to_journal()`
   уже реализует в бою, просто применённая ТОЛЬКО к send_scheduled-записям.
3. Дедуп по `journal_id` — максимальный `ts` СРЕДИ send_scheduled-записей
   (не среди всех 5 типов, как непреднамеренно делает `_dedup_by_trade()`
   сейчас в проде — см. "Побочная находка" ниже).

Данные подняты локально (git archive `origin/main` → journal/ во
временную директорию), вычисления НЕ выполнялись на боевом контейнере
(правило CLAUDE.md "тяжёлые проверки не в боевом контейнере").

## Сколько сделок реально имели shadow-поля

- **Снимок 1 (битый матчинг): 16 из 22** (73%) — 6 ложно-null (ZEC, AKT,
  US#2, CFG, UB, VVV) + BEAT (честный n/д, до фичи live_journal_id).
- **Снимок 2 (исправленный матчинг): 21 из 22** (95%) — только BEAT
  остаётся честным н/д (сделка 2026-07-12 03:51, ДО того как
  `live_journal_id`-связка вообще появилась в коде, см. `git log -S`
  `f5755b71`, 2026-07-12 10:11 +03 — не баг, физически не могло быть
  иначе).

Число сделок в базе осталось **22** в обоих снимках (гейт n=22
подтверждён, дедуп по исправленной логике даёт те же 22 `journal_id`, что
и раньше — состав выборки не изменился, изменилось только КАКАЯ shadow-
запись привязана к каждой сделке).

## Таблица СТАРЫЕ (Снимок 1, битый матчинг) vs ЧИСТЫЕ (Снимок 2, исправленный)

| Признак | Группа | СТАРОЕ (n, WR%, PF) | ЧИСТОЕ (n, WR%, PF) | Изменилось? |
|---|---|---|---|---|
| **AMD-фаза** (сгруппир.) | manipulation/dead_zone | n=6, WR=66.7%, PF=4.29 | n=8, WR=50.0%, PF=2.14 | WR/PF ниже на большем n — направление то же, эффект слабее чем казалось |
| | accumulation/distribution | n=9, WR=0.0%, PF=0.00 | n=13, WR=0.0%, PF=0.00 | не изменилось (0% держится на большем n — сильнее как анти-сигнал) |
| | (нет данных — BEAT) | n=7 (6 ложно+1 честно) | n=1 (только BEAT, честно) | подтверждает: 6 из 7 старых "н/д" были артефактом |
| **inducement_swept** | True | n=8, WR=37.5%, PF=1.38 | n=11, WR=27.3%, PF=0.86 | лифт меньше на полном n (+17.3пп вместо +23.2пп) |
| | False | n=7, WR=14.3%, PF=0.27 | n=10, WR=10.0%, PF=0.18 | база чуть хуже, направление то же |
| **tz13_setup_type** | AMD | n=4, WR=50.0%, PF=1.83 | n=6, WR=33.3%, PF=0.91 | слабее на большем n, но всё ещё выше базы (18.2%) |
| | Sweep | n=4, WR=0.0%, PF=0.00 | n=4, WR=0.0%, PF=0.00 | не изменилось (анти-сигнал держится, тот же n) |
| | SH-BOS-RTO | (не было видно отдельно — было в "None") | n=2, WR=0.0%, PF=0.00 | НОВАЯ подкатегория, вскрылась только после фикса матчинга |
| **whale_klvl_confluence** | False | n=16, WR=25.0%, PF=0.71 | **n=22, WR=18.2%, PF=0.48** | теперь на ВСЕХ 22 сделках, не на 16 |
| | True | n=0 (никогда) | **n=0 (никогда)** | **не изменилось — см. вывод ниже** |

Базовая выборка (все 22, без разбивки): WR=18.2%, PF=0.48 — идентично в
обоих снимках (ожидаемо, это просто n=22 сделок из журнала, состав не
менялся исправлением матчинга).

## Вывод по каждому пункту ТЗ

**AMD-фаза** — кандидат ВЫЖИВАЕТ после фикса, но слабее, чем казалось на
неполных данных: WR разрыв 50.0% vs 0.0% (было 66.7% vs 0.0%). На полном
n=22 картина ЧИЩЕ (меньше "шума" от неправильно классифицированных null-
записей), но эффект скромнее. Остаётся топ-кандидатом.

**inducement_swept** — кандидат выживает, лифт снизился с +23.2пп до
+17.3пп на полном n. Направление стабильно (True лучше False), величина
эффекта была слегка переоценена на грязных данных.

**tz13_setup_type** — Sweep как анти-сигнал ПОДТВЕРЖДЁН без изменений
(0% и на n=4 в обоих снимках — эти 4 сделки не входили в число 6
неправильно смэтченных). AMD-подтип слабее на полном n, но выше базы.
Новая подкатегория SH-BOS-RTO (n=2) вскрылась только сейчас — честно
n<5, наблюдать дальше.

## Побочная находка при расследовании (за рамками этого ТЗ, но существенно)

`shadow_outcome_analysis._dedup_by_trade()` (боевой код, используется
`closed_outcomes_report()` → `tools/morning_brief.py`/`morning_metrics.py`
для min_outcomes=20-гейта) дедуплицирует по МАКСИМАЛЬНОМУ `ts` СРЕДИ ВСЕХ
типов shadow-записей, сматченных на один `journal_id` — включая 4 Фаза-B
auto_*_shadow-записи без source-фильтра. Т.к. `auto_onchain_shadow`
пишется ПОСЛЕДНИМ в цикле `send_scheduled()` (см. `SHADOW_REGRESSION_
INVESTIGATION.md`), у него САМЫЙ БОЛЬШОЙ `ts` в кластере — значит
`_dedup_by_trade()` в ПРОДЕ, вероятно, тоже выбирает пустую auto_onchain_
shadow-запись вместо содержательной send_scheduled-записи для КАЖДОЙ
сделки, где Фаза-B контуры включены (с ~2026-07-18). Это НЕ трогалось в
рамках этой задачи (код/гейты — под запретом), но означает, что
`CONTOUR_PRESENCE_FIELDS`-подсчёт (`tz13`/`patch05_bpr`/`patch09_oi`
готовность к min_outcomes=20 в утреннем брифе) вероятно ЗАНИЖЕН с
07-18 — реальных сделок с данными по контуру больше, чем показывает
текущий боевой подсчёт. **Кандидат на отдельную задачу** (тот же класс
фикса — добавить `source == "send_scheduled"` фильтр перед дедупом в
`closed_outcomes_by_contour()`), не входит в объём этого расследования.

## whale_klvl_confluence — реально мёртв, не жертва битого матчинга

Проверено на ЧИСТЫХ данных: `False` на всех 22/22 сделок, `True` — 0 раз.
До фикса матчинга это было видно только на 16/22 (маскировка возможна в
принципе), но исправление добавило ИМЕННО те 6 сделок, где раньше стояло
None — и ВСЕ 6 тоже показывают `False`, не `True`. Вывод не изменился и
ПОДТВЕРЖДЁН на полном n: `whale_klvl_confluence` — мёртвый фильтр по
факту данных, не артефакт матчинга. Кандидат на отдельное расследование
"почему никогда не True" остаётся в силе (пункт 4 итогового списка
Снимка 1).

## MFE/MAE A/B вердикт на чистой выборке

Поле `mfe_price`/`mae_price` пишется в `journal/signals.json` напрямую
(не через shadow-матчинг) — битый матчинг НИКОГДА не мог задеть эти
данные. Пересчёт на исправленной привязке journal_id→shadow даёт ТЕ ЖЕ
8 сделок с MFE/MAE (TRUMP/NIGHT/ZEC/AKT/US#2/CFG/UB/VVV), тот же результат
по убыткам (n=7, исключая NIGHT-win): **A (MFE≥+1R) = 3 (AKT/US#2/UB),
B (MFE<+0.3R) = 3 (TRUMP/CFG/VVV), середина = 1 (ZEC)** — идентично
Снимку 1. Подтверждает: MFE/MAE-часть анализа НИКОГДА не была
контаминирована багом матчинга, вердикт "неопределённо на этом n"
остаётся в силе без изменений.

## Обновлённый итоговый список кандидатов (заменяет пункт 5 Снимка 1)

1. AMD-фаза (сгруппир.) — топ-кандидат, эффект скромнее на полном n
   (50% vs 0%, было 66.7% vs 0%). Продолжать копить.
2. `inducement_swept` — лифт +17.3пп на полном n (был +23.2пп). Продолжать копить.
3. `tz13_setup_type=Sweep` — анти-сигнал подтверждён без изменений (0%, n=4).
4. `whale_klvl_confluence` — **подтверждённый мёртвый фильтр на чистых
   данных (22/22 False)**, не артефакт. Кандидат на отдельное расследование "почему".
5. ~~Разрыв shadow-данных для 6 сделок~~ — ЗАКРЫТО, см.
   `SHADOW_REGRESSION_INVESTIGATION.md`: был баг матчинга в скрипте, не
   регресс пайплайна. Новый пункт вместо этого: `_dedup_by_trade()` в
   `shadow_outcome_analysis.py` вероятно занижает контурную готовность к
   min_outcomes=20 с ~07-18 (см. "Побочная находка" выше) — кандидат на
   отдельную задачу, код НЕ трогался.
6. MFE/MAE A/B — без изменений, неопределённо на n=7 (3/3/1).

**Ничего из вышеперечисленного не применено к боевому чек-листу/гейтам —
только кандидаты, решение за владельцем. Код/live/гейты/флаги не менялись
в рамках этой задачи. `PAUSE_LIVE_SIGNAL_EMISSION` оставлен `true`.**
